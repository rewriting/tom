module iptables.Ast
imports String int
abstract syntax

  Rules = rules(Rule*)

  Rule = rule(action:Action,
              source:Address,
              destination:Address)

  Action = Accept()
         | Drop()
       
  Address = Addr(ip:String)


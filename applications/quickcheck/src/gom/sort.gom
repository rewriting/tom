module Sort
imports int
abstract syntax
Expr = zero()
     | un()
     | plus(a:Expr, b:Expr)
     | mult(a:Expr, b:Expr)

Surexpr = rien()
        | wrapper(e:Expr)
        | rec(s:Surexpr)

IntList = nill()
        | consList(a:int, tail:IntList)

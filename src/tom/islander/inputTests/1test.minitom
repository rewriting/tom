%match(MyType subject, MySecondType s2) { 
  _ -> { underscore }
  _* -> { underscore.Star }
  _,_* -> { underscore.Underscore.Star }
  !aa -> { anti.aa }
  myAnnotation@_ -> { annotated.Underscore}
  anno@!bb -> { anno.at.anti.bb }
}

%match(newSubject){
 _ -> { secondMatch }
}

{
  %match(newSubject){
    _ -> { thirdMatch }
  }
}

Simple:   show how to define a mapping for the Java version of the
          ATerm Library 
          This example introduces the notion of associative
          operator as well as list-matching

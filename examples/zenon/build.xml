<?xml version="1.0" encoding="UTF-8"?>
<project name="Build the zenon example" default="build" basedir="..">
  <import file="../common.xml"/>
  <property name="zenon" value="zenon"/>
  <target name="clean" description="Cleans examples directory">
    <delete dir="${examples.gen}/${zenon}"/>
    <delete dir="${examples.build}/${zenon}"/>
  </target>
  <target name="build" depends="init">
    <tom.preset srcdir="${examples.dir}"
                destdir="${examples.gen}"
                options="-I ${tom.home}/share/jtom --verify">
      <include name="${zenon}/**/*.t"/>
      <exclude name="${zenon}/**/*.g.t"/>
    </tom.preset>
    <javacompile example="${zenon}"/>
    <antcall target="zenon.make"/>
  </target>
  <target name="zenon.make" if="zvtov.bin">
    <apply dir="${examples.gen}/${zenon}" 
           dest="${examples.gen}/${zenon}"
           failonerror="yes"
	   executable="${zvtov.bin}">
      <srcfile/>
      <fileset dir="${examples.gen}">
        <include name="${zenon}/**/*.zv"/>
      </fileset>
      <mapper type="glob" from="*.zv" to="*.v"/>
    </apply>
  </target>
  <target name="dist" depends="prepare.examples" if="distrib.zip">
    <zip destfile="${distrib.zip}" 
         update="yes">
      <zipfileset dir="${examples.dir}" prefix="examples" filemode="644">
        <include name="${zenon}/*.t"/>
        <include name="${zenon}/*.coz"/>
        <include name="${zenon}/*.v"/>
        <include name="${zenon}/*.zv"/>
      </zipfileset>
    </zip>
  </target>
</project>

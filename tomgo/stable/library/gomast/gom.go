// Code generated by tom/tomgo backend from Gom. DO NOT EDIT.
package gomast

import (
	"fmt"
	"strings"

	"tom/tomgo/stable/library/sharedobjects"
)

// underscore-prevent: tolerate unused imports if a module has no slots of these types.
var _ = fmt.Sprintf
var _ = strings.Join

// Option is the Go interface backing the Gom sort Option.
type Option interface {
	sharedobjects.Term
	isOption()
}

// OriginOption is the term type for the alternative `Origin(...)` of sort Option.
type OriginOption struct {
	Line int64
	hash uint32
}

func (*OriginOption) isOption() {}

func (t *OriginOption) Hash() uint32 { return t.hash }

func (t *OriginOption) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*OriginOption)
	if !ok {
		return false
	}
	if t.Line != o.Line {
		return false
	}
	return true
}

func (t *OriginOption) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *OriginOption) String() string {
	return fmt.Sprintf("Origin(%v)", t.Line)
}

// MakeOrigin builds the canonical (shared) Origin term.
func MakeOrigin(line int64) Option {
	hashes := []uint32{sharedobjects.StringHash(fmt.Sprintf("%v", line))}
	proto := &OriginOption{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("Origin"), hashes), Line: line}
	return factory.Build(proto).(*OriginOption)
}

// OptionListOption is the term type for the alternative `OptionList(...)` of sort Option.
type OptionListOption struct {
	Slots []Option
	hash  uint32
}

func (*OptionListOption) isOption() {}

func (t *OptionListOption) Hash() uint32 { return t.hash }

func (t *OptionListOption) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*OptionListOption)
	if !ok {
		return false
	}
	if len(t.Slots) != len(o.Slots) {
		return false
	}
	for i := range t.Slots {
		if t.Slots[i] != o.Slots[i] {
			return false
		}
	}
	return true
}

func (t *OptionListOption) Duplicate() sharedobjects.Term {
	cp := append([]Option(nil), t.Slots...)
	return &OptionListOption{Slots: cp, hash: t.hash}
}

func (t *OptionListOption) String() string {
	parts := make([]string, len(t.Slots))
	for i, v := range t.Slots {
		parts[i] = fmt.Sprintf("%v", v)
	}
	return "OptionList" + "(" + strings.Join(parts, ",") + ")"
}

// MakeOptionList builds the canonical (shared) OptionList term.
func MakeOptionList(args ...Option) Option {
	hashes := make([]uint32, 0, len(args))
	for _, a := range args {
		hashes = append(hashes, a.Hash())
	}
	proto := &OptionListOption{Slots: args, hash: sharedobjects.MixSymbol(sharedobjects.StringHash("OptionList"), hashes)}
	return factory.Build(proto).(*OptionListOption)
}

// HasTomCodeOption is the term type for the alternative `HasTomCode(...)` of sort Option.
type HasTomCodeOption struct {
	Value bool
	hash  uint32
}

func (*HasTomCodeOption) isOption() {}

func (t *HasTomCodeOption) Hash() uint32 { return t.hash }

func (t *HasTomCodeOption) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*HasTomCodeOption)
	if !ok {
		return false
	}
	if t.Value != o.Value {
		return false
	}
	return true
}

func (t *HasTomCodeOption) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *HasTomCodeOption) String() string {
	return fmt.Sprintf("HasTomCode(%v)", t.Value)
}

// MakeHasTomCode builds the canonical (shared) HasTomCode term.
func MakeHasTomCode(value bool) Option {
	hashes := []uint32{sharedobjects.StringHash(fmt.Sprintf("%v", value))}
	proto := &HasTomCodeOption{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("HasTomCode"), hashes), Value: value}
	return factory.Build(proto).(*HasTomCodeOption)
}

// DetailsOption is the term type for the alternative `Details(...)` of sort Option.
type DetailsOption struct {
	Comments string
	hash     uint32
}

func (*DetailsOption) isOption() {}

func (t *DetailsOption) Hash() uint32 { return t.hash }

func (t *DetailsOption) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*DetailsOption)
	if !ok {
		return false
	}
	if t.Comments != o.Comments {
		return false
	}
	return true
}

func (t *DetailsOption) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *DetailsOption) String() string {
	return fmt.Sprintf("Details(%s)", sharedobjects.JavaEscape(t.Comments))
}

// MakeDetails builds the canonical (shared) Details term.
func MakeDetails(comments string) Option {
	hashes := []uint32{sharedobjects.StringHash(fmt.Sprintf("%v", comments))}
	proto := &DetailsOption{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("Details"), hashes), Comments: comments}
	return factory.Build(proto).(*DetailsOption)
}

// GomModule is the Go interface backing the Gom sort GomModule.
type GomModule interface {
	sharedobjects.Term
	isGomModule()
}

// GomModuleGomModule is the term type for the alternative `GomModule(...)` of sort GomModule.
type GomModuleGomModule struct {
	ModuleName  GomModuleName
	SectionList SectionList
	hash        uint32
}

func (*GomModuleGomModule) isGomModule() {}

func (t *GomModuleGomModule) Hash() uint32 { return t.hash }

func (t *GomModuleGomModule) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*GomModuleGomModule)
	if !ok {
		return false
	}
	if t.ModuleName != o.ModuleName {
		return false
	}
	if t.SectionList != o.SectionList {
		return false
	}
	return true
}

func (t *GomModuleGomModule) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *GomModuleGomModule) String() string {
	return fmt.Sprintf("GomModule(%v,%v)", t.ModuleName, t.SectionList)
}

// MakeGomModule builds the canonical (shared) GomModule term.
func MakeGomModule(moduleName GomModuleName, sectionList SectionList) GomModule {
	hashes := []uint32{moduleName.Hash(), sectionList.Hash()}
	proto := &GomModuleGomModule{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("GomModule"), hashes), ModuleName: moduleName, SectionList: sectionList}
	return factory.Build(proto).(*GomModuleGomModule)
}

// GomModuleList is the Go interface backing the Gom sort GomModuleList.
type GomModuleList interface {
	sharedobjects.Term
	isGomModuleList()
}

// ConcGomModuleGomModuleList is the term type for the alternative `ConcGomModule(...)` of sort GomModuleList.
type ConcGomModuleGomModuleList struct {
	Slots []GomModule
	hash  uint32
}

func (*ConcGomModuleGomModuleList) isGomModuleList() {}

func (t *ConcGomModuleGomModuleList) Hash() uint32 { return t.hash }

func (t *ConcGomModuleGomModuleList) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ConcGomModuleGomModuleList)
	if !ok {
		return false
	}
	if len(t.Slots) != len(o.Slots) {
		return false
	}
	for i := range t.Slots {
		if t.Slots[i] != o.Slots[i] {
			return false
		}
	}
	return true
}

func (t *ConcGomModuleGomModuleList) Duplicate() sharedobjects.Term {
	cp := append([]GomModule(nil), t.Slots...)
	return &ConcGomModuleGomModuleList{Slots: cp, hash: t.hash}
}

func (t *ConcGomModuleGomModuleList) String() string {
	parts := make([]string, len(t.Slots))
	for i, v := range t.Slots {
		parts[i] = fmt.Sprintf("%v", v)
	}
	return "ConcGomModule" + "(" + strings.Join(parts, ",") + ")"
}

// MakeConcGomModule builds the canonical (shared) ConcGomModule term.
func MakeConcGomModule(args ...GomModule) GomModuleList {
	hashes := make([]uint32, 0, len(args))
	for _, a := range args {
		hashes = append(hashes, a.Hash())
	}
	proto := &ConcGomModuleGomModuleList{Slots: args, hash: sharedobjects.MixSymbol(sharedobjects.StringHash("ConcGomModule"), hashes)}
	return factory.Build(proto).(*ConcGomModuleGomModuleList)
}

// Section is the Go interface backing the Gom sort Section.
type Section interface {
	sharedobjects.Term
	isSection()
}

// ImportsSection is the term type for the alternative `Imports(...)` of sort Section.
type ImportsSection struct {
	ImportList ImportList
	hash       uint32
}

func (*ImportsSection) isSection() {}

func (t *ImportsSection) Hash() uint32 { return t.hash }

func (t *ImportsSection) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ImportsSection)
	if !ok {
		return false
	}
	if t.ImportList != o.ImportList {
		return false
	}
	return true
}

func (t *ImportsSection) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *ImportsSection) String() string {
	return fmt.Sprintf("Imports(%v)", t.ImportList)
}

// MakeImports builds the canonical (shared) Imports term.
func MakeImports(importList ImportList) Section {
	hashes := []uint32{importList.Hash()}
	proto := &ImportsSection{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("Imports"), hashes), ImportList: importList}
	return factory.Build(proto).(*ImportsSection)
}

// PublicSection is the term type for the alternative `Public(...)` of sort Section.
type PublicSection struct {
	ProductionList ProductionList
	hash           uint32
}

func (*PublicSection) isSection() {}

func (t *PublicSection) Hash() uint32 { return t.hash }

func (t *PublicSection) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*PublicSection)
	if !ok {
		return false
	}
	if t.ProductionList != o.ProductionList {
		return false
	}
	return true
}

func (t *PublicSection) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *PublicSection) String() string {
	return fmt.Sprintf("Public(%v)", t.ProductionList)
}

// MakePublic builds the canonical (shared) Public term.
func MakePublic(productionList ProductionList) Section {
	hashes := []uint32{productionList.Hash()}
	proto := &PublicSection{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("Public"), hashes), ProductionList: productionList}
	return factory.Build(proto).(*PublicSection)
}

// SectionList is the Go interface backing the Gom sort SectionList.
type SectionList interface {
	sharedobjects.Term
	isSectionList()
}

// ConcSectionSectionList is the term type for the alternative `ConcSection(...)` of sort SectionList.
type ConcSectionSectionList struct {
	Slots []Section
	hash  uint32
}

func (*ConcSectionSectionList) isSectionList() {}

func (t *ConcSectionSectionList) Hash() uint32 { return t.hash }

func (t *ConcSectionSectionList) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ConcSectionSectionList)
	if !ok {
		return false
	}
	if len(t.Slots) != len(o.Slots) {
		return false
	}
	for i := range t.Slots {
		if t.Slots[i] != o.Slots[i] {
			return false
		}
	}
	return true
}

func (t *ConcSectionSectionList) Duplicate() sharedobjects.Term {
	cp := append([]Section(nil), t.Slots...)
	return &ConcSectionSectionList{Slots: cp, hash: t.hash}
}

func (t *ConcSectionSectionList) String() string {
	parts := make([]string, len(t.Slots))
	for i, v := range t.Slots {
		parts[i] = fmt.Sprintf("%v", v)
	}
	return "ConcSection" + "(" + strings.Join(parts, ",") + ")"
}

// MakeConcSection builds the canonical (shared) ConcSection term.
func MakeConcSection(args ...Section) SectionList {
	hashes := make([]uint32, 0, len(args))
	for _, a := range args {
		hashes = append(hashes, a.Hash())
	}
	proto := &ConcSectionSectionList{Slots: args, hash: sharedobjects.MixSymbol(sharedobjects.StringHash("ConcSection"), hashes)}
	return factory.Build(proto).(*ConcSectionSectionList)
}

// ImportList is the Go interface backing the Gom sort ImportList.
type ImportList interface {
	sharedobjects.Term
	isImportList()
}

// ConcImportedModuleImportList is the term type for the alternative `ConcImportedModule(...)` of sort ImportList.
type ConcImportedModuleImportList struct {
	Slots []GomModuleName
	hash  uint32
}

func (*ConcImportedModuleImportList) isImportList() {}

func (t *ConcImportedModuleImportList) Hash() uint32 { return t.hash }

func (t *ConcImportedModuleImportList) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ConcImportedModuleImportList)
	if !ok {
		return false
	}
	if len(t.Slots) != len(o.Slots) {
		return false
	}
	for i := range t.Slots {
		if t.Slots[i] != o.Slots[i] {
			return false
		}
	}
	return true
}

func (t *ConcImportedModuleImportList) Duplicate() sharedobjects.Term {
	cp := append([]GomModuleName(nil), t.Slots...)
	return &ConcImportedModuleImportList{Slots: cp, hash: t.hash}
}

func (t *ConcImportedModuleImportList) String() string {
	parts := make([]string, len(t.Slots))
	for i, v := range t.Slots {
		parts[i] = fmt.Sprintf("%v", v)
	}
	return "ConcImportedModule" + "(" + strings.Join(parts, ",") + ")"
}

// MakeConcImportedModule builds the canonical (shared) ConcImportedModule term.
func MakeConcImportedModule(args ...GomModuleName) ImportList {
	hashes := make([]uint32, 0, len(args))
	for _, a := range args {
		hashes = append(hashes, a.Hash())
	}
	proto := &ConcImportedModuleImportList{Slots: args, hash: sharedobjects.MixSymbol(sharedobjects.StringHash("ConcImportedModule"), hashes)}
	return factory.Build(proto).(*ConcImportedModuleImportList)
}

// Production is the Go interface backing the Gom sort Production.
type Production interface {
	sharedobjects.Term
	isProduction()
}

// HookProduction is the term type for the alternative `Hook(...)` of sort Production.
type HookProduction struct {
	NameType    IdKind
	Name        string
	HookType    HookKind
	Args        ArgList
	HookContent HookContent
	Option      Option
	hash        uint32
}

func (*HookProduction) isProduction() {}

func (t *HookProduction) Hash() uint32 { return t.hash }

func (t *HookProduction) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*HookProduction)
	if !ok {
		return false
	}
	if t.NameType != o.NameType {
		return false
	}
	if t.Name != o.Name {
		return false
	}
	if t.HookType != o.HookType {
		return false
	}
	if t.Args != o.Args {
		return false
	}
	if t.HookContent != o.HookContent {
		return false
	}
	if t.Option != o.Option {
		return false
	}
	return true
}

func (t *HookProduction) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *HookProduction) String() string {
	return fmt.Sprintf("Hook(%v,%s,%v,%v,%v,%v)", t.NameType, sharedobjects.JavaEscape(t.Name), t.HookType, t.Args, t.HookContent, t.Option)
}

// MakeHook builds the canonical (shared) Hook term.
func MakeHook(nameType IdKind, name string, hookType HookKind, args ArgList, hookContent HookContent, option Option) Production {
	hashes := []uint32{nameType.Hash(), sharedobjects.StringHash(fmt.Sprintf("%v", name)), hookType.Hash(), args.Hash(), hookContent.Hash(), option.Hash()}
	proto := &HookProduction{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("Hook"), hashes), NameType: nameType, Name: name, HookType: hookType, Args: args, HookContent: hookContent, Option: option}
	return factory.Build(proto).(*HookProduction)
}

// SortTypeProduction is the term type for the alternative `SortType(...)` of sort Production.
type SortTypeProduction struct {
	Type            GomType
	Binds           AtomList
	AlternativeList AlternativeList
	hash            uint32
}

func (*SortTypeProduction) isProduction() {}

func (t *SortTypeProduction) Hash() uint32 { return t.hash }

func (t *SortTypeProduction) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*SortTypeProduction)
	if !ok {
		return false
	}
	if t.Type != o.Type {
		return false
	}
	if t.Binds != o.Binds {
		return false
	}
	if t.AlternativeList != o.AlternativeList {
		return false
	}
	return true
}

func (t *SortTypeProduction) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *SortTypeProduction) String() string {
	return fmt.Sprintf("SortType(%v,%v,%v)", t.Type, t.Binds, t.AlternativeList)
}

// MakeSortType builds the canonical (shared) SortType term.
func MakeSortType(type_ GomType, binds AtomList, alternativeList AlternativeList) Production {
	hashes := []uint32{type_.Hash(), binds.Hash(), alternativeList.Hash()}
	proto := &SortTypeProduction{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("SortType"), hashes), Type: type_, Binds: binds, AlternativeList: alternativeList}
	return factory.Build(proto).(*SortTypeProduction)
}

// AtomDeclProduction is the term type for the alternative `AtomDecl(...)` of sort Production.
type AtomDeclProduction struct {
	Name string
	hash uint32
}

func (*AtomDeclProduction) isProduction() {}

func (t *AtomDeclProduction) Hash() uint32 { return t.hash }

func (t *AtomDeclProduction) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*AtomDeclProduction)
	if !ok {
		return false
	}
	if t.Name != o.Name {
		return false
	}
	return true
}

func (t *AtomDeclProduction) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *AtomDeclProduction) String() string {
	return fmt.Sprintf("AtomDecl(%s)", sharedobjects.JavaEscape(t.Name))
}

// MakeAtomDecl builds the canonical (shared) AtomDecl term.
func MakeAtomDecl(name string) Production {
	hashes := []uint32{sharedobjects.StringHash(fmt.Sprintf("%v", name))}
	proto := &AtomDeclProduction{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("AtomDecl"), hashes), Name: name}
	return factory.Build(proto).(*AtomDeclProduction)
}

// ProductionList is the Go interface backing the Gom sort ProductionList.
type ProductionList interface {
	sharedobjects.Term
	isProductionList()
}

// ConcProductionProductionList is the term type for the alternative `ConcProduction(...)` of sort ProductionList.
type ConcProductionProductionList struct {
	Slots []Production
	hash  uint32
}

func (*ConcProductionProductionList) isProductionList() {}

func (t *ConcProductionProductionList) Hash() uint32 { return t.hash }

func (t *ConcProductionProductionList) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ConcProductionProductionList)
	if !ok {
		return false
	}
	if len(t.Slots) != len(o.Slots) {
		return false
	}
	for i := range t.Slots {
		if t.Slots[i] != o.Slots[i] {
			return false
		}
	}
	return true
}

func (t *ConcProductionProductionList) Duplicate() sharedobjects.Term {
	cp := append([]Production(nil), t.Slots...)
	return &ConcProductionProductionList{Slots: cp, hash: t.hash}
}

func (t *ConcProductionProductionList) String() string {
	parts := make([]string, len(t.Slots))
	for i, v := range t.Slots {
		parts[i] = fmt.Sprintf("%v", v)
	}
	return "ConcProduction" + "(" + strings.Join(parts, ",") + ")"
}

// MakeConcProduction builds the canonical (shared) ConcProduction term.
func MakeConcProduction(args ...Production) ProductionList {
	hashes := make([]uint32, 0, len(args))
	for _, a := range args {
		hashes = append(hashes, a.Hash())
	}
	proto := &ConcProductionProductionList{Slots: args, hash: sharedobjects.MixSymbol(sharedobjects.StringHash("ConcProduction"), hashes)}
	return factory.Build(proto).(*ConcProductionProductionList)
}

// AlternativeList is the Go interface backing the Gom sort AlternativeList.
type AlternativeList interface {
	sharedobjects.Term
	isAlternativeList()
}

// ConcAlternativeAlternativeList is the term type for the alternative `ConcAlternative(...)` of sort AlternativeList.
type ConcAlternativeAlternativeList struct {
	Slots []Alternative
	hash  uint32
}

func (*ConcAlternativeAlternativeList) isAlternativeList() {}

func (t *ConcAlternativeAlternativeList) Hash() uint32 { return t.hash }

func (t *ConcAlternativeAlternativeList) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ConcAlternativeAlternativeList)
	if !ok {
		return false
	}
	if len(t.Slots) != len(o.Slots) {
		return false
	}
	for i := range t.Slots {
		if t.Slots[i] != o.Slots[i] {
			return false
		}
	}
	return true
}

func (t *ConcAlternativeAlternativeList) Duplicate() sharedobjects.Term {
	cp := append([]Alternative(nil), t.Slots...)
	return &ConcAlternativeAlternativeList{Slots: cp, hash: t.hash}
}

func (t *ConcAlternativeAlternativeList) String() string {
	parts := make([]string, len(t.Slots))
	for i, v := range t.Slots {
		parts[i] = fmt.Sprintf("%v", v)
	}
	return "ConcAlternative" + "(" + strings.Join(parts, ",") + ")"
}

// MakeConcAlternative builds the canonical (shared) ConcAlternative term.
func MakeConcAlternative(args ...Alternative) AlternativeList {
	hashes := make([]uint32, 0, len(args))
	for _, a := range args {
		hashes = append(hashes, a.Hash())
	}
	proto := &ConcAlternativeAlternativeList{Slots: args, hash: sharedobjects.MixSymbol(sharedobjects.StringHash("ConcAlternative"), hashes)}
	return factory.Build(proto).(*ConcAlternativeAlternativeList)
}

// Alternative is the Go interface backing the Gom sort Alternative.
type Alternative interface {
	sharedobjects.Term
	isAlternative()
}

// AlternativeAlternative is the term type for the alternative `Alternative(...)` of sort Alternative.
type AlternativeAlternative struct {
	Name       string
	DomainList FieldList
	Codomain   GomType
	Option     Option
	hash       uint32
}

func (*AlternativeAlternative) isAlternative() {}

func (t *AlternativeAlternative) Hash() uint32 { return t.hash }

func (t *AlternativeAlternative) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*AlternativeAlternative)
	if !ok {
		return false
	}
	if t.Name != o.Name {
		return false
	}
	if t.DomainList != o.DomainList {
		return false
	}
	if t.Codomain != o.Codomain {
		return false
	}
	if t.Option != o.Option {
		return false
	}
	return true
}

func (t *AlternativeAlternative) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *AlternativeAlternative) String() string {
	return fmt.Sprintf("Alternative(%s,%v,%v,%v)", sharedobjects.JavaEscape(t.Name), t.DomainList, t.Codomain, t.Option)
}

// MakeAlternative builds the canonical (shared) Alternative term.
func MakeAlternative(name string, domainList FieldList, codomain GomType, option Option) Alternative {
	hashes := []uint32{sharedobjects.StringHash(fmt.Sprintf("%v", name)), domainList.Hash(), codomain.Hash(), option.Hash()}
	proto := &AlternativeAlternative{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("Alternative"), hashes), Name: name, DomainList: domainList, Codomain: codomain, Option: option}
	return factory.Build(proto).(*AlternativeAlternative)
}

// HookKind is the Go interface backing the Gom sort HookKind.
type HookKind interface {
	sharedobjects.Term
	isHookKind()
}

// HookKindHookKind is the term type for the alternative `HookKind(...)` of sort HookKind.
type HookKindHookKind struct {
	Kind string
	hash uint32
}

func (*HookKindHookKind) isHookKind() {}

func (t *HookKindHookKind) Hash() uint32 { return t.hash }

func (t *HookKindHookKind) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*HookKindHookKind)
	if !ok {
		return false
	}
	if t.Kind != o.Kind {
		return false
	}
	return true
}

func (t *HookKindHookKind) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *HookKindHookKind) String() string {
	return fmt.Sprintf("HookKind(%s)", sharedobjects.JavaEscape(t.Kind))
}

// MakeHookKind builds the canonical (shared) HookKind term.
func MakeHookKind(kind string) HookKind {
	hashes := []uint32{sharedobjects.StringHash(fmt.Sprintf("%v", kind))}
	proto := &HookKindHookKind{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("HookKind"), hashes), Kind: kind}
	return factory.Build(proto).(*HookKindHookKind)
}

// IdKind is the Go interface backing the Gom sort IdKind.
type IdKind interface {
	sharedobjects.Term
	isIdKind()
}

// KindModuleIdKind is the term type for the alternative `KindModule(...)` of sort IdKind.
type KindModuleIdKind struct {
	hash uint32
}

func (*KindModuleIdKind) isIdKind() {}

func (t *KindModuleIdKind) Hash() uint32 { return t.hash }

func (t *KindModuleIdKind) Equivalent(other sharedobjects.Term) bool {
	_, ok := other.(*KindModuleIdKind)
	return ok
}

func (t *KindModuleIdKind) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *KindModuleIdKind) String() string {
	return "KindModule" + "()"
}

// MakeKindModule builds the canonical (shared) KindModule term.
func MakeKindModule() IdKind {
	hashes := []uint32{}
	proto := &KindModuleIdKind{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("KindModule"), hashes)}
	return factory.Build(proto).(*KindModuleIdKind)
}

// KindSortIdKind is the term type for the alternative `KindSort(...)` of sort IdKind.
type KindSortIdKind struct {
	hash uint32
}

func (*KindSortIdKind) isIdKind() {}

func (t *KindSortIdKind) Hash() uint32 { return t.hash }

func (t *KindSortIdKind) Equivalent(other sharedobjects.Term) bool {
	_, ok := other.(*KindSortIdKind)
	return ok
}

func (t *KindSortIdKind) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *KindSortIdKind) String() string {
	return "KindSort" + "()"
}

// MakeKindSort builds the canonical (shared) KindSort term.
func MakeKindSort() IdKind {
	hashes := []uint32{}
	proto := &KindSortIdKind{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("KindSort"), hashes)}
	return factory.Build(proto).(*KindSortIdKind)
}

// KindOperatorIdKind is the term type for the alternative `KindOperator(...)` of sort IdKind.
type KindOperatorIdKind struct {
	hash uint32
}

func (*KindOperatorIdKind) isIdKind() {}

func (t *KindOperatorIdKind) Hash() uint32 { return t.hash }

func (t *KindOperatorIdKind) Equivalent(other sharedobjects.Term) bool {
	_, ok := other.(*KindOperatorIdKind)
	return ok
}

func (t *KindOperatorIdKind) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *KindOperatorIdKind) String() string {
	return "KindOperator" + "()"
}

// MakeKindOperator builds the canonical (shared) KindOperator term.
func MakeKindOperator() IdKind {
	hashes := []uint32{}
	proto := &KindOperatorIdKind{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("KindOperator"), hashes)}
	return factory.Build(proto).(*KindOperatorIdKind)
}

// KindFutureOperatorIdKind is the term type for the alternative `KindFutureOperator(...)` of sort IdKind.
type KindFutureOperatorIdKind struct {
	ConsOrNil Future
	hash      uint32
}

func (*KindFutureOperatorIdKind) isIdKind() {}

func (t *KindFutureOperatorIdKind) Hash() uint32 { return t.hash }

func (t *KindFutureOperatorIdKind) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*KindFutureOperatorIdKind)
	if !ok {
		return false
	}
	if t.ConsOrNil != o.ConsOrNil {
		return false
	}
	return true
}

func (t *KindFutureOperatorIdKind) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *KindFutureOperatorIdKind) String() string {
	return fmt.Sprintf("KindFutureOperator(%v)", t.ConsOrNil)
}

// MakeKindFutureOperator builds the canonical (shared) KindFutureOperator term.
func MakeKindFutureOperator(consOrNil Future) IdKind {
	hashes := []uint32{consOrNil.Hash()}
	proto := &KindFutureOperatorIdKind{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("KindFutureOperator"), hashes), ConsOrNil: consOrNil}
	return factory.Build(proto).(*KindFutureOperatorIdKind)
}

// Arg is the Go interface backing the Gom sort Arg.
type Arg interface {
	sharedobjects.Term
	isArg()
}

// ArgArg is the term type for the alternative `Arg(...)` of sort Arg.
type ArgArg struct {
	Name string
	hash uint32
}

func (*ArgArg) isArg() {}

func (t *ArgArg) Hash() uint32 { return t.hash }

func (t *ArgArg) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ArgArg)
	if !ok {
		return false
	}
	if t.Name != o.Name {
		return false
	}
	return true
}

func (t *ArgArg) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *ArgArg) String() string {
	return fmt.Sprintf("Arg(%s)", sharedobjects.JavaEscape(t.Name))
}

// MakeArg builds the canonical (shared) Arg term.
func MakeArg(name string) Arg {
	hashes := []uint32{sharedobjects.StringHash(fmt.Sprintf("%v", name))}
	proto := &ArgArg{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("Arg"), hashes), Name: name}
	return factory.Build(proto).(*ArgArg)
}

// ArgList is the Go interface backing the Gom sort ArgList.
type ArgList interface {
	sharedobjects.Term
	isArgList()
}

// ConcArgArgList is the term type for the alternative `ConcArg(...)` of sort ArgList.
type ConcArgArgList struct {
	Slots []Arg
	hash  uint32
}

func (*ConcArgArgList) isArgList() {}

func (t *ConcArgArgList) Hash() uint32 { return t.hash }

func (t *ConcArgArgList) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ConcArgArgList)
	if !ok {
		return false
	}
	if len(t.Slots) != len(o.Slots) {
		return false
	}
	for i := range t.Slots {
		if t.Slots[i] != o.Slots[i] {
			return false
		}
	}
	return true
}

func (t *ConcArgArgList) Duplicate() sharedobjects.Term {
	cp := append([]Arg(nil), t.Slots...)
	return &ConcArgArgList{Slots: cp, hash: t.hash}
}

func (t *ConcArgArgList) String() string {
	parts := make([]string, len(t.Slots))
	for i, v := range t.Slots {
		parts[i] = fmt.Sprintf("%v", v)
	}
	return "ConcArg" + "(" + strings.Join(parts, ",") + ")"
}

// MakeConcArg builds the canonical (shared) ConcArg term.
func MakeConcArg(args ...Arg) ArgList {
	hashes := make([]uint32, 0, len(args))
	for _, a := range args {
		hashes = append(hashes, a.Hash())
	}
	proto := &ConcArgArgList{Slots: args, hash: sharedobjects.MixSymbol(sharedobjects.StringHash("ConcArg"), hashes)}
	return factory.Build(proto).(*ConcArgArgList)
}

// ScopeSpecifier is the Go interface backing the Gom sort ScopeSpecifier.
type ScopeSpecifier interface {
	sharedobjects.Term
	isScopeSpecifier()
}

// NoneScopeSpecifier is the term type for the alternative `None(...)` of sort ScopeSpecifier.
type NoneScopeSpecifier struct {
	hash uint32
}

func (*NoneScopeSpecifier) isScopeSpecifier() {}

func (t *NoneScopeSpecifier) Hash() uint32 { return t.hash }

func (t *NoneScopeSpecifier) Equivalent(other sharedobjects.Term) bool {
	_, ok := other.(*NoneScopeSpecifier)
	return ok
}

func (t *NoneScopeSpecifier) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *NoneScopeSpecifier) String() string {
	return "None" + "()"
}

// MakeNone builds the canonical (shared) None term.
func MakeNone() ScopeSpecifier {
	hashes := []uint32{}
	proto := &NoneScopeSpecifier{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("None"), hashes)}
	return factory.Build(proto).(*NoneScopeSpecifier)
}

// InnerScopeSpecifier is the term type for the alternative `Inner(...)` of sort ScopeSpecifier.
type InnerScopeSpecifier struct {
	hash uint32
}

func (*InnerScopeSpecifier) isScopeSpecifier() {}

func (t *InnerScopeSpecifier) Hash() uint32 { return t.hash }

func (t *InnerScopeSpecifier) Equivalent(other sharedobjects.Term) bool {
	_, ok := other.(*InnerScopeSpecifier)
	return ok
}

func (t *InnerScopeSpecifier) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *InnerScopeSpecifier) String() string {
	return "Inner" + "()"
}

// MakeInner builds the canonical (shared) Inner term.
func MakeInner() ScopeSpecifier {
	hashes := []uint32{}
	proto := &InnerScopeSpecifier{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("Inner"), hashes)}
	return factory.Build(proto).(*InnerScopeSpecifier)
}

// OuterScopeSpecifier is the term type for the alternative `Outer(...)` of sort ScopeSpecifier.
type OuterScopeSpecifier struct {
	hash uint32
}

func (*OuterScopeSpecifier) isScopeSpecifier() {}

func (t *OuterScopeSpecifier) Hash() uint32 { return t.hash }

func (t *OuterScopeSpecifier) Equivalent(other sharedobjects.Term) bool {
	_, ok := other.(*OuterScopeSpecifier)
	return ok
}

func (t *OuterScopeSpecifier) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *OuterScopeSpecifier) String() string {
	return "Outer" + "()"
}

// MakeOuter builds the canonical (shared) Outer term.
func MakeOuter() ScopeSpecifier {
	hashes := []uint32{}
	proto := &OuterScopeSpecifier{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("Outer"), hashes)}
	return factory.Build(proto).(*OuterScopeSpecifier)
}

// NeutralScopeSpecifier is the term type for the alternative `Neutral(...)` of sort ScopeSpecifier.
type NeutralScopeSpecifier struct {
	hash uint32
}

func (*NeutralScopeSpecifier) isScopeSpecifier() {}

func (t *NeutralScopeSpecifier) Hash() uint32 { return t.hash }

func (t *NeutralScopeSpecifier) Equivalent(other sharedobjects.Term) bool {
	_, ok := other.(*NeutralScopeSpecifier)
	return ok
}

func (t *NeutralScopeSpecifier) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *NeutralScopeSpecifier) String() string {
	return "Neutral" + "()"
}

// MakeNeutral builds the canonical (shared) Neutral term.
func MakeNeutral() ScopeSpecifier {
	hashes := []uint32{}
	proto := &NeutralScopeSpecifier{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("Neutral"), hashes)}
	return factory.Build(proto).(*NeutralScopeSpecifier)
}

// RefreshScopeSpecifier is the term type for the alternative `Refresh(...)` of sort ScopeSpecifier.
type RefreshScopeSpecifier struct {
	hash uint32
}

func (*RefreshScopeSpecifier) isScopeSpecifier() {}

func (t *RefreshScopeSpecifier) Hash() uint32 { return t.hash }

func (t *RefreshScopeSpecifier) Equivalent(other sharedobjects.Term) bool {
	_, ok := other.(*RefreshScopeSpecifier)
	return ok
}

func (t *RefreshScopeSpecifier) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *RefreshScopeSpecifier) String() string {
	return "Refresh" + "()"
}

// MakeRefresh builds the canonical (shared) Refresh term.
func MakeRefresh() ScopeSpecifier {
	hashes := []uint32{}
	proto := &RefreshScopeSpecifier{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("Refresh"), hashes)}
	return factory.Build(proto).(*RefreshScopeSpecifier)
}

// Field is the Go interface backing the Gom sort Field.
type Field interface {
	sharedobjects.Term
	isField()
}

// StarredFieldField is the term type for the alternative `StarredField(...)` of sort Field.
type StarredFieldField struct {
	FieldType GomType
	Specifier ScopeSpecifier
	hash      uint32
}

func (*StarredFieldField) isField() {}

func (t *StarredFieldField) Hash() uint32 { return t.hash }

func (t *StarredFieldField) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*StarredFieldField)
	if !ok {
		return false
	}
	if t.FieldType != o.FieldType {
		return false
	}
	if t.Specifier != o.Specifier {
		return false
	}
	return true
}

func (t *StarredFieldField) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *StarredFieldField) String() string {
	return fmt.Sprintf("StarredField(%v,%v)", t.FieldType, t.Specifier)
}

// MakeStarredField builds the canonical (shared) StarredField term.
func MakeStarredField(fieldType GomType, specifier ScopeSpecifier) Field {
	hashes := []uint32{fieldType.Hash(), specifier.Hash()}
	proto := &StarredFieldField{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("StarredField"), hashes), FieldType: fieldType, Specifier: specifier}
	return factory.Build(proto).(*StarredFieldField)
}

// NamedFieldField is the term type for the alternative `NamedField(...)` of sort Field.
type NamedFieldField struct {
	Name      string
	FieldType GomType
	Specifier ScopeSpecifier
	hash      uint32
}

func (*NamedFieldField) isField() {}

func (t *NamedFieldField) Hash() uint32 { return t.hash }

func (t *NamedFieldField) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*NamedFieldField)
	if !ok {
		return false
	}
	if t.Name != o.Name {
		return false
	}
	if t.FieldType != o.FieldType {
		return false
	}
	if t.Specifier != o.Specifier {
		return false
	}
	return true
}

func (t *NamedFieldField) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *NamedFieldField) String() string {
	return fmt.Sprintf("NamedField(%s,%v,%v)", sharedobjects.JavaEscape(t.Name), t.FieldType, t.Specifier)
}

// MakeNamedField builds the canonical (shared) NamedField term.
func MakeNamedField(name string, fieldType GomType, specifier ScopeSpecifier) Field {
	hashes := []uint32{sharedobjects.StringHash(fmt.Sprintf("%v", name)), fieldType.Hash(), specifier.Hash()}
	proto := &NamedFieldField{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("NamedField"), hashes), Name: name, FieldType: fieldType, Specifier: specifier}
	return factory.Build(proto).(*NamedFieldField)
}

// FieldList is the Go interface backing the Gom sort FieldList.
type FieldList interface {
	sharedobjects.Term
	isFieldList()
}

// ConcFieldFieldList is the term type for the alternative `ConcField(...)` of sort FieldList.
type ConcFieldFieldList struct {
	Slots []Field
	hash  uint32
}

func (*ConcFieldFieldList) isFieldList() {}

func (t *ConcFieldFieldList) Hash() uint32 { return t.hash }

func (t *ConcFieldFieldList) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ConcFieldFieldList)
	if !ok {
		return false
	}
	if len(t.Slots) != len(o.Slots) {
		return false
	}
	for i := range t.Slots {
		if t.Slots[i] != o.Slots[i] {
			return false
		}
	}
	return true
}

func (t *ConcFieldFieldList) Duplicate() sharedobjects.Term {
	cp := append([]Field(nil), t.Slots...)
	return &ConcFieldFieldList{Slots: cp, hash: t.hash}
}

func (t *ConcFieldFieldList) String() string {
	parts := make([]string, len(t.Slots))
	for i, v := range t.Slots {
		parts[i] = fmt.Sprintf("%v", v)
	}
	return "ConcField" + "(" + strings.Join(parts, ",") + ")"
}

// MakeConcField builds the canonical (shared) ConcField term.
func MakeConcField(args ...Field) FieldList {
	hashes := make([]uint32, 0, len(args))
	for _, a := range args {
		hashes = append(hashes, a.Hash())
	}
	proto := &ConcFieldFieldList{Slots: args, hash: sharedobjects.MixSymbol(sharedobjects.StringHash("ConcField"), hashes)}
	return factory.Build(proto).(*ConcFieldFieldList)
}

// GomModuleName is the Go interface backing the Gom sort GomModuleName.
type GomModuleName interface {
	sharedobjects.Term
	isGomModuleName()
}

// GomModuleNameGomModuleName is the term type for the alternative `GomModuleName(...)` of sort GomModuleName.
type GomModuleNameGomModuleName struct {
	Name string
	hash uint32
}

func (*GomModuleNameGomModuleName) isGomModuleName() {}

func (t *GomModuleNameGomModuleName) Hash() uint32 { return t.hash }

func (t *GomModuleNameGomModuleName) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*GomModuleNameGomModuleName)
	if !ok {
		return false
	}
	if t.Name != o.Name {
		return false
	}
	return true
}

func (t *GomModuleNameGomModuleName) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *GomModuleNameGomModuleName) String() string {
	return fmt.Sprintf("GomModuleName(%s)", sharedobjects.JavaEscape(t.Name))
}

// MakeGomModuleName builds the canonical (shared) GomModuleName term.
func MakeGomModuleName(name string) GomModuleName {
	hashes := []uint32{sharedobjects.StringHash(fmt.Sprintf("%v", name))}
	proto := &GomModuleNameGomModuleName{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("GomModuleName"), hashes), Name: name}
	return factory.Build(proto).(*GomModuleNameGomModuleName)
}

// TypeSpec is the Go interface backing the Gom sort TypeSpec.
type TypeSpec interface {
	sharedobjects.Term
	isTypeSpec()
}

// PatternTypeTypeSpec is the term type for the alternative `PatternType(...)` of sort TypeSpec.
type PatternTypeTypeSpec struct {
	hash uint32
}

func (*PatternTypeTypeSpec) isTypeSpec() {}

func (t *PatternTypeTypeSpec) Hash() uint32 { return t.hash }

func (t *PatternTypeTypeSpec) Equivalent(other sharedobjects.Term) bool {
	_, ok := other.(*PatternTypeTypeSpec)
	return ok
}

func (t *PatternTypeTypeSpec) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *PatternTypeTypeSpec) String() string {
	return "PatternType" + "()"
}

// MakePatternType builds the canonical (shared) PatternType term.
func MakePatternType() TypeSpec {
	hashes := []uint32{}
	proto := &PatternTypeTypeSpec{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("PatternType"), hashes)}
	return factory.Build(proto).(*PatternTypeTypeSpec)
}

// ExpressionTypeTypeSpec is the term type for the alternative `ExpressionType(...)` of sort TypeSpec.
type ExpressionTypeTypeSpec struct {
	hash uint32
}

func (*ExpressionTypeTypeSpec) isTypeSpec() {}

func (t *ExpressionTypeTypeSpec) Hash() uint32 { return t.hash }

func (t *ExpressionTypeTypeSpec) Equivalent(other sharedobjects.Term) bool {
	_, ok := other.(*ExpressionTypeTypeSpec)
	return ok
}

func (t *ExpressionTypeTypeSpec) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *ExpressionTypeTypeSpec) String() string {
	return "ExpressionType" + "()"
}

// MakeExpressionType builds the canonical (shared) ExpressionType term.
func MakeExpressionType() TypeSpec {
	hashes := []uint32{}
	proto := &ExpressionTypeTypeSpec{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("ExpressionType"), hashes)}
	return factory.Build(proto).(*ExpressionTypeTypeSpec)
}

// AtomTypeTypeSpec is the term type for the alternative `AtomType(...)` of sort TypeSpec.
type AtomTypeTypeSpec struct {
	hash uint32
}

func (*AtomTypeTypeSpec) isTypeSpec() {}

func (t *AtomTypeTypeSpec) Hash() uint32 { return t.hash }

func (t *AtomTypeTypeSpec) Equivalent(other sharedobjects.Term) bool {
	_, ok := other.(*AtomTypeTypeSpec)
	return ok
}

func (t *AtomTypeTypeSpec) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *AtomTypeTypeSpec) String() string {
	return "AtomType" + "()"
}

// MakeAtomType builds the canonical (shared) AtomType term.
func MakeAtomType() TypeSpec {
	hashes := []uint32{}
	proto := &AtomTypeTypeSpec{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("AtomType"), hashes)}
	return factory.Build(proto).(*AtomTypeTypeSpec)
}

// GomType is the Go interface backing the Gom sort GomType.
type GomType interface {
	sharedobjects.Term
	isGomType()
}

// GomTypeGomType is the term type for the alternative `GomType(...)` of sort GomType.
type GomTypeGomType struct {
	Specialization TypeSpec
	Name           string
	hash           uint32
}

func (*GomTypeGomType) isGomType() {}

func (t *GomTypeGomType) Hash() uint32 { return t.hash }

func (t *GomTypeGomType) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*GomTypeGomType)
	if !ok {
		return false
	}
	if t.Specialization != o.Specialization {
		return false
	}
	if t.Name != o.Name {
		return false
	}
	return true
}

func (t *GomTypeGomType) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *GomTypeGomType) String() string {
	return fmt.Sprintf("GomType(%v,%s)", t.Specialization, sharedobjects.JavaEscape(t.Name))
}

// MakeGomType builds the canonical (shared) GomType term.
func MakeGomType(specialization TypeSpec, name string) GomType {
	hashes := []uint32{specialization.Hash(), sharedobjects.StringHash(fmt.Sprintf("%v", name))}
	proto := &GomTypeGomType{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("GomType"), hashes), Specialization: specialization, Name: name}
	return factory.Build(proto).(*GomTypeGomType)
}

// GomTypeList is the Go interface backing the Gom sort GomTypeList.
type GomTypeList interface {
	sharedobjects.Term
	isGomTypeList()
}

// ConcGomTypeGomTypeList is the term type for the alternative `ConcGomType(...)` of sort GomTypeList.
type ConcGomTypeGomTypeList struct {
	Slots []GomType
	hash  uint32
}

func (*ConcGomTypeGomTypeList) isGomTypeList() {}

func (t *ConcGomTypeGomTypeList) Hash() uint32 { return t.hash }

func (t *ConcGomTypeGomTypeList) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ConcGomTypeGomTypeList)
	if !ok {
		return false
	}
	if len(t.Slots) != len(o.Slots) {
		return false
	}
	for i := range t.Slots {
		if t.Slots[i] != o.Slots[i] {
			return false
		}
	}
	return true
}

func (t *ConcGomTypeGomTypeList) Duplicate() sharedobjects.Term {
	cp := append([]GomType(nil), t.Slots...)
	return &ConcGomTypeGomTypeList{Slots: cp, hash: t.hash}
}

func (t *ConcGomTypeGomTypeList) String() string {
	parts := make([]string, len(t.Slots))
	for i, v := range t.Slots {
		parts[i] = fmt.Sprintf("%v", v)
	}
	return "ConcGomType" + "(" + strings.Join(parts, ",") + ")"
}

// MakeConcGomType builds the canonical (shared) ConcGomType term.
func MakeConcGomType(args ...GomType) GomTypeList {
	hashes := make([]uint32, 0, len(args))
	for _, a := range args {
		hashes = append(hashes, a.Hash())
	}
	proto := &ConcGomTypeGomTypeList{Slots: args, hash: sharedobjects.MixSymbol(sharedobjects.StringHash("ConcGomType"), hashes)}
	return factory.Build(proto).(*ConcGomTypeGomTypeList)
}

// AtomList is the Go interface backing the Gom sort AtomList.
type AtomList interface {
	sharedobjects.Term
	isAtomList()
}

// ConcAtomAtomList is the term type for the alternative `ConcAtom(...)` of sort AtomList.
type ConcAtomAtomList struct {
	Slots []string
	hash  uint32
}

func (*ConcAtomAtomList) isAtomList() {}

func (t *ConcAtomAtomList) Hash() uint32 { return t.hash }

func (t *ConcAtomAtomList) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ConcAtomAtomList)
	if !ok {
		return false
	}
	if len(t.Slots) != len(o.Slots) {
		return false
	}
	for i := range t.Slots {
		if t.Slots[i] != o.Slots[i] {
			return false
		}
	}
	return true
}

func (t *ConcAtomAtomList) Duplicate() sharedobjects.Term {
	cp := append([]string(nil), t.Slots...)
	return &ConcAtomAtomList{Slots: cp, hash: t.hash}
}

func (t *ConcAtomAtomList) String() string {
	parts := make([]string, len(t.Slots))
	for i, v := range t.Slots {
		parts[i] = fmt.Sprintf("%s", sharedobjects.JavaEscape(v))
	}
	return "ConcAtom" + "(" + strings.Join(parts, ",") + ")"
}

// MakeConcAtom builds the canonical (shared) ConcAtom term.
func MakeConcAtom(args ...string) AtomList {
	hashes := make([]uint32, 0, len(args))
	for _, a := range args {
		hashes = append(hashes, sharedobjects.StringHash(fmt.Sprintf("%v", a)))
	}
	proto := &ConcAtomAtomList{Slots: args, hash: sharedobjects.MixSymbol(sharedobjects.StringHash("ConcAtom"), hashes)}
	return factory.Build(proto).(*ConcAtomAtomList)
}

// Module is the Go interface backing the Gom sort Module.
type Module interface {
	sharedobjects.Term
	isModule()
}

// ModuleModule is the term type for the alternative `Module(...)` of sort Module.
type ModuleModule struct {
	MDecl ModuleDecl
	Sorts SortList
	hash  uint32
}

func (*ModuleModule) isModule() {}

func (t *ModuleModule) Hash() uint32 { return t.hash }

func (t *ModuleModule) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ModuleModule)
	if !ok {
		return false
	}
	if t.MDecl != o.MDecl {
		return false
	}
	if t.Sorts != o.Sorts {
		return false
	}
	return true
}

func (t *ModuleModule) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *ModuleModule) String() string {
	return fmt.Sprintf("Module(%v,%v)", t.MDecl, t.Sorts)
}

// MakeModule builds the canonical (shared) Module term.
func MakeModule(mDecl ModuleDecl, sorts SortList) Module {
	hashes := []uint32{mDecl.Hash(), sorts.Hash()}
	proto := &ModuleModule{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("Module"), hashes), MDecl: mDecl, Sorts: sorts}
	return factory.Build(proto).(*ModuleModule)
}

// ModuleList is the Go interface backing the Gom sort ModuleList.
type ModuleList interface {
	sharedobjects.Term
	isModuleList()
}

// ConcModuleModuleList is the term type for the alternative `ConcModule(...)` of sort ModuleList.
type ConcModuleModuleList struct {
	Slots []Module
	hash  uint32
}

func (*ConcModuleModuleList) isModuleList() {}

func (t *ConcModuleModuleList) Hash() uint32 { return t.hash }

func (t *ConcModuleModuleList) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ConcModuleModuleList)
	if !ok {
		return false
	}
	if len(t.Slots) != len(o.Slots) {
		return false
	}
	for i := range t.Slots {
		if t.Slots[i] != o.Slots[i] {
			return false
		}
	}
	return true
}

func (t *ConcModuleModuleList) Duplicate() sharedobjects.Term {
	cp := append([]Module(nil), t.Slots...)
	return &ConcModuleModuleList{Slots: cp, hash: t.hash}
}

func (t *ConcModuleModuleList) String() string {
	parts := make([]string, len(t.Slots))
	for i, v := range t.Slots {
		parts[i] = fmt.Sprintf("%v", v)
	}
	return "ConcModule" + "(" + strings.Join(parts, ",") + ")"
}

// MakeConcModule builds the canonical (shared) ConcModule term.
func MakeConcModule(args ...Module) ModuleList {
	hashes := make([]uint32, 0, len(args))
	for _, a := range args {
		hashes = append(hashes, a.Hash())
	}
	proto := &ConcModuleModuleList{Slots: args, hash: sharedobjects.MixSymbol(sharedobjects.StringHash("ConcModule"), hashes)}
	return factory.Build(proto).(*ConcModuleModuleList)
}

// ModuleDecl is the Go interface backing the Gom sort ModuleDecl.
type ModuleDecl interface {
	sharedobjects.Term
	isModuleDecl()
}

// ModuleDeclModuleDecl is the term type for the alternative `ModuleDecl(...)` of sort ModuleDecl.
type ModuleDeclModuleDecl struct {
	ModuleName GomModuleName
	Pkg        string
	hash       uint32
}

func (*ModuleDeclModuleDecl) isModuleDecl() {}

func (t *ModuleDeclModuleDecl) Hash() uint32 { return t.hash }

func (t *ModuleDeclModuleDecl) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ModuleDeclModuleDecl)
	if !ok {
		return false
	}
	if t.ModuleName != o.ModuleName {
		return false
	}
	if t.Pkg != o.Pkg {
		return false
	}
	return true
}

func (t *ModuleDeclModuleDecl) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *ModuleDeclModuleDecl) String() string {
	return fmt.Sprintf("ModuleDecl(%v,%s)", t.ModuleName, sharedobjects.JavaEscape(t.Pkg))
}

// MakeModuleDecl builds the canonical (shared) ModuleDecl term.
func MakeModuleDecl(moduleName GomModuleName, pkg string) ModuleDecl {
	hashes := []uint32{moduleName.Hash(), sharedobjects.StringHash(fmt.Sprintf("%v", pkg))}
	proto := &ModuleDeclModuleDecl{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("ModuleDecl"), hashes), ModuleName: moduleName, Pkg: pkg}
	return factory.Build(proto).(*ModuleDeclModuleDecl)
}

// ModuleDeclList is the Go interface backing the Gom sort ModuleDeclList.
type ModuleDeclList interface {
	sharedobjects.Term
	isModuleDeclList()
}

// ConcModuleDeclModuleDeclList is the term type for the alternative `ConcModuleDecl(...)` of sort ModuleDeclList.
type ConcModuleDeclModuleDeclList struct {
	Slots []ModuleDecl
	hash  uint32
}

func (*ConcModuleDeclModuleDeclList) isModuleDeclList() {}

func (t *ConcModuleDeclModuleDeclList) Hash() uint32 { return t.hash }

func (t *ConcModuleDeclModuleDeclList) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ConcModuleDeclModuleDeclList)
	if !ok {
		return false
	}
	if len(t.Slots) != len(o.Slots) {
		return false
	}
	for i := range t.Slots {
		if t.Slots[i] != o.Slots[i] {
			return false
		}
	}
	return true
}

func (t *ConcModuleDeclModuleDeclList) Duplicate() sharedobjects.Term {
	cp := append([]ModuleDecl(nil), t.Slots...)
	return &ConcModuleDeclModuleDeclList{Slots: cp, hash: t.hash}
}

func (t *ConcModuleDeclModuleDeclList) String() string {
	parts := make([]string, len(t.Slots))
	for i, v := range t.Slots {
		parts[i] = fmt.Sprintf("%v", v)
	}
	return "ConcModuleDecl" + "(" + strings.Join(parts, ",") + ")"
}

// MakeConcModuleDecl builds the canonical (shared) ConcModuleDecl term.
func MakeConcModuleDecl(args ...ModuleDecl) ModuleDeclList {
	hashes := make([]uint32, 0, len(args))
	for _, a := range args {
		hashes = append(hashes, a.Hash())
	}
	proto := &ConcModuleDeclModuleDeclList{Slots: args, hash: sharedobjects.MixSymbol(sharedobjects.StringHash("ConcModuleDecl"), hashes)}
	return factory.Build(proto).(*ConcModuleDeclModuleDeclList)
}

// SortDecl is the Go interface backing the Gom sort SortDecl.
type SortDecl interface {
	sharedobjects.Term
	isSortDecl()
}

// SortDeclSortDecl is the term type for the alternative `SortDecl(...)` of sort SortDecl.
type SortDeclSortDecl struct {
	Name       string
	ModuleDecl ModuleDecl
	hash       uint32
}

func (*SortDeclSortDecl) isSortDecl() {}

func (t *SortDeclSortDecl) Hash() uint32 { return t.hash }

func (t *SortDeclSortDecl) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*SortDeclSortDecl)
	if !ok {
		return false
	}
	if t.Name != o.Name {
		return false
	}
	if t.ModuleDecl != o.ModuleDecl {
		return false
	}
	return true
}

func (t *SortDeclSortDecl) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *SortDeclSortDecl) String() string {
	return fmt.Sprintf("SortDecl(%s,%v)", sharedobjects.JavaEscape(t.Name), t.ModuleDecl)
}

// MakeSortDecl builds the canonical (shared) SortDecl term.
func MakeSortDecl(name string, moduleDecl ModuleDecl) SortDecl {
	hashes := []uint32{sharedobjects.StringHash(fmt.Sprintf("%v", name)), moduleDecl.Hash()}
	proto := &SortDeclSortDecl{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("SortDecl"), hashes), Name: name, ModuleDecl: moduleDecl}
	return factory.Build(proto).(*SortDeclSortDecl)
}

// BuiltinSortDeclSortDecl is the term type for the alternative `BuiltinSortDecl(...)` of sort SortDecl.
type BuiltinSortDeclSortDecl struct {
	Name string
	hash uint32
}

func (*BuiltinSortDeclSortDecl) isSortDecl() {}

func (t *BuiltinSortDeclSortDecl) Hash() uint32 { return t.hash }

func (t *BuiltinSortDeclSortDecl) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*BuiltinSortDeclSortDecl)
	if !ok {
		return false
	}
	if t.Name != o.Name {
		return false
	}
	return true
}

func (t *BuiltinSortDeclSortDecl) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *BuiltinSortDeclSortDecl) String() string {
	return fmt.Sprintf("BuiltinSortDecl(%s)", sharedobjects.JavaEscape(t.Name))
}

// MakeBuiltinSortDecl builds the canonical (shared) BuiltinSortDecl term.
func MakeBuiltinSortDecl(name string) SortDecl {
	hashes := []uint32{sharedobjects.StringHash(fmt.Sprintf("%v", name))}
	proto := &BuiltinSortDeclSortDecl{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("BuiltinSortDecl"), hashes), Name: name}
	return factory.Build(proto).(*BuiltinSortDeclSortDecl)
}

// SortDeclList is the Go interface backing the Gom sort SortDeclList.
type SortDeclList interface {
	sharedobjects.Term
	isSortDeclList()
}

// ConcSortDeclSortDeclList is the term type for the alternative `ConcSortDecl(...)` of sort SortDeclList.
type ConcSortDeclSortDeclList struct {
	Slots []SortDecl
	hash  uint32
}

func (*ConcSortDeclSortDeclList) isSortDeclList() {}

func (t *ConcSortDeclSortDeclList) Hash() uint32 { return t.hash }

func (t *ConcSortDeclSortDeclList) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ConcSortDeclSortDeclList)
	if !ok {
		return false
	}
	if len(t.Slots) != len(o.Slots) {
		return false
	}
	for i := range t.Slots {
		if t.Slots[i] != o.Slots[i] {
			return false
		}
	}
	return true
}

func (t *ConcSortDeclSortDeclList) Duplicate() sharedobjects.Term {
	cp := append([]SortDecl(nil), t.Slots...)
	return &ConcSortDeclSortDeclList{Slots: cp, hash: t.hash}
}

func (t *ConcSortDeclSortDeclList) String() string {
	parts := make([]string, len(t.Slots))
	for i, v := range t.Slots {
		parts[i] = fmt.Sprintf("%v", v)
	}
	return "ConcSortDecl" + "(" + strings.Join(parts, ",") + ")"
}

// MakeConcSortDecl builds the canonical (shared) ConcSortDecl term.
func MakeConcSortDecl(args ...SortDecl) SortDeclList {
	hashes := make([]uint32, 0, len(args))
	for _, a := range args {
		hashes = append(hashes, a.Hash())
	}
	proto := &ConcSortDeclSortDeclList{Slots: args, hash: sharedobjects.MixSymbol(sharedobjects.StringHash("ConcSortDecl"), hashes)}
	return factory.Build(proto).(*ConcSortDeclSortDeclList)
}

// OperatorDecl is the Go interface backing the Gom sort OperatorDecl.
type OperatorDecl interface {
	sharedobjects.Term
	isOperatorDecl()
}

// OperatorDeclOperatorDecl is the term type for the alternative `OperatorDecl(...)` of sort OperatorDecl.
type OperatorDeclOperatorDecl struct {
	Name   string
	Sort   SortDecl
	Prod   TypedProduction
	Option Option
	hash   uint32
}

func (*OperatorDeclOperatorDecl) isOperatorDecl() {}

func (t *OperatorDeclOperatorDecl) Hash() uint32 { return t.hash }

func (t *OperatorDeclOperatorDecl) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*OperatorDeclOperatorDecl)
	if !ok {
		return false
	}
	if t.Name != o.Name {
		return false
	}
	if t.Sort != o.Sort {
		return false
	}
	if t.Prod != o.Prod {
		return false
	}
	if t.Option != o.Option {
		return false
	}
	return true
}

func (t *OperatorDeclOperatorDecl) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *OperatorDeclOperatorDecl) String() string {
	return fmt.Sprintf("OperatorDecl(%s,%v,%v,%v)", sharedobjects.JavaEscape(t.Name), t.Sort, t.Prod, t.Option)
}

// MakeOperatorDecl builds the canonical (shared) OperatorDecl term.
func MakeOperatorDecl(name string, sort SortDecl, prod TypedProduction, option Option) OperatorDecl {
	hashes := []uint32{sharedobjects.StringHash(fmt.Sprintf("%v", name)), sort.Hash(), prod.Hash(), option.Hash()}
	proto := &OperatorDeclOperatorDecl{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("OperatorDecl"), hashes), Name: name, Sort: sort, Prod: prod, Option: option}
	return factory.Build(proto).(*OperatorDeclOperatorDecl)
}

// OperatorDeclList is the Go interface backing the Gom sort OperatorDeclList.
type OperatorDeclList interface {
	sharedobjects.Term
	isOperatorDeclList()
}

// ConcOperatorOperatorDeclList is the term type for the alternative `ConcOperator(...)` of sort OperatorDeclList.
type ConcOperatorOperatorDeclList struct {
	Slots []OperatorDecl
	hash  uint32
}

func (*ConcOperatorOperatorDeclList) isOperatorDeclList() {}

func (t *ConcOperatorOperatorDeclList) Hash() uint32 { return t.hash }

func (t *ConcOperatorOperatorDeclList) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ConcOperatorOperatorDeclList)
	if !ok {
		return false
	}
	if len(t.Slots) != len(o.Slots) {
		return false
	}
	for i := range t.Slots {
		if t.Slots[i] != o.Slots[i] {
			return false
		}
	}
	return true
}

func (t *ConcOperatorOperatorDeclList) Duplicate() sharedobjects.Term {
	cp := append([]OperatorDecl(nil), t.Slots...)
	return &ConcOperatorOperatorDeclList{Slots: cp, hash: t.hash}
}

func (t *ConcOperatorOperatorDeclList) String() string {
	parts := make([]string, len(t.Slots))
	for i, v := range t.Slots {
		parts[i] = fmt.Sprintf("%v", v)
	}
	return "ConcOperator" + "(" + strings.Join(parts, ",") + ")"
}

// MakeConcOperator builds the canonical (shared) ConcOperator term.
func MakeConcOperator(args ...OperatorDecl) OperatorDeclList {
	hashes := make([]uint32, 0, len(args))
	for _, a := range args {
		hashes = append(hashes, a.Hash())
	}
	proto := &ConcOperatorOperatorDeclList{Slots: args, hash: sharedobjects.MixSymbol(sharedobjects.StringHash("ConcOperator"), hashes)}
	return factory.Build(proto).(*ConcOperatorOperatorDeclList)
}

// TypedProduction is the Go interface backing the Gom sort TypedProduction.
type TypedProduction interface {
	sharedobjects.Term
	isTypedProduction()
}

// SlotsTypedProduction is the term type for the alternative `Slots(...)` of sort TypedProduction.
type SlotsTypedProduction struct {
	Slots SlotList
	hash  uint32
}

func (*SlotsTypedProduction) isTypedProduction() {}

func (t *SlotsTypedProduction) Hash() uint32 { return t.hash }

func (t *SlotsTypedProduction) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*SlotsTypedProduction)
	if !ok {
		return false
	}
	if t.Slots != o.Slots {
		return false
	}
	return true
}

func (t *SlotsTypedProduction) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *SlotsTypedProduction) String() string {
	return fmt.Sprintf("Slots(%v)", t.Slots)
}

// MakeSlots builds the canonical (shared) Slots term.
func MakeSlots(slots SlotList) TypedProduction {
	hashes := []uint32{slots.Hash()}
	proto := &SlotsTypedProduction{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("Slots"), hashes), Slots: slots}
	return factory.Build(proto).(*SlotsTypedProduction)
}

// VariadicTypedProduction is the term type for the alternative `Variadic(...)` of sort TypedProduction.
type VariadicTypedProduction struct {
	Sort SortDecl
	hash uint32
}

func (*VariadicTypedProduction) isTypedProduction() {}

func (t *VariadicTypedProduction) Hash() uint32 { return t.hash }

func (t *VariadicTypedProduction) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*VariadicTypedProduction)
	if !ok {
		return false
	}
	if t.Sort != o.Sort {
		return false
	}
	return true
}

func (t *VariadicTypedProduction) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *VariadicTypedProduction) String() string {
	return fmt.Sprintf("Variadic(%v)", t.Sort)
}

// MakeVariadic builds the canonical (shared) Variadic term.
func MakeVariadic(sort SortDecl) TypedProduction {
	hashes := []uint32{sort.Hash()}
	proto := &VariadicTypedProduction{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("Variadic"), hashes), Sort: sort}
	return factory.Build(proto).(*VariadicTypedProduction)
}

// Slot is the Go interface backing the Gom sort Slot.
type Slot interface {
	sharedobjects.Term
	isSlot()
}

// SlotSlot is the term type for the alternative `Slot(...)` of sort Slot.
type SlotSlot struct {
	Name string
	Sort SortDecl
	hash uint32
}

func (*SlotSlot) isSlot() {}

func (t *SlotSlot) Hash() uint32 { return t.hash }

func (t *SlotSlot) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*SlotSlot)
	if !ok {
		return false
	}
	if t.Name != o.Name {
		return false
	}
	if t.Sort != o.Sort {
		return false
	}
	return true
}

func (t *SlotSlot) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *SlotSlot) String() string {
	return fmt.Sprintf("Slot(%s,%v)", sharedobjects.JavaEscape(t.Name), t.Sort)
}

// MakeSlot builds the canonical (shared) Slot term.
func MakeSlot(name string, sort SortDecl) Slot {
	hashes := []uint32{sharedobjects.StringHash(fmt.Sprintf("%v", name)), sort.Hash()}
	proto := &SlotSlot{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("Slot"), hashes), Name: name, Sort: sort}
	return factory.Build(proto).(*SlotSlot)
}

// SlotList is the Go interface backing the Gom sort SlotList.
type SlotList interface {
	sharedobjects.Term
	isSlotList()
}

// ConcSlotSlotList is the term type for the alternative `ConcSlot(...)` of sort SlotList.
type ConcSlotSlotList struct {
	Slots []Slot
	hash  uint32
}

func (*ConcSlotSlotList) isSlotList() {}

func (t *ConcSlotSlotList) Hash() uint32 { return t.hash }

func (t *ConcSlotSlotList) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ConcSlotSlotList)
	if !ok {
		return false
	}
	if len(t.Slots) != len(o.Slots) {
		return false
	}
	for i := range t.Slots {
		if t.Slots[i] != o.Slots[i] {
			return false
		}
	}
	return true
}

func (t *ConcSlotSlotList) Duplicate() sharedobjects.Term {
	cp := append([]Slot(nil), t.Slots...)
	return &ConcSlotSlotList{Slots: cp, hash: t.hash}
}

func (t *ConcSlotSlotList) String() string {
	parts := make([]string, len(t.Slots))
	for i, v := range t.Slots {
		parts[i] = fmt.Sprintf("%v", v)
	}
	return "ConcSlot" + "(" + strings.Join(parts, ",") + ")"
}

// MakeConcSlot builds the canonical (shared) ConcSlot term.
func MakeConcSlot(args ...Slot) SlotList {
	hashes := make([]uint32, 0, len(args))
	for _, a := range args {
		hashes = append(hashes, a.Hash())
	}
	proto := &ConcSlotSlotList{Slots: args, hash: sharedobjects.MixSymbol(sharedobjects.StringHash("ConcSlot"), hashes)}
	return factory.Build(proto).(*ConcSlotSlotList)
}

// Sort is the Go interface backing the Gom sort Sort.
type Sort interface {
	sharedobjects.Term
	isSort()
}

// SortSort is the term type for the alternative `Sort(...)` of sort Sort.
type SortSort struct {
	Decl          SortDecl
	OperatorDecls OperatorDeclList
	hash          uint32
}

func (*SortSort) isSort() {}

func (t *SortSort) Hash() uint32 { return t.hash }

func (t *SortSort) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*SortSort)
	if !ok {
		return false
	}
	if t.Decl != o.Decl {
		return false
	}
	if t.OperatorDecls != o.OperatorDecls {
		return false
	}
	return true
}

func (t *SortSort) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *SortSort) String() string {
	return fmt.Sprintf("Sort(%v,%v)", t.Decl, t.OperatorDecls)
}

// MakeSort builds the canonical (shared) Sort term.
func MakeSort(decl SortDecl, operatorDecls OperatorDeclList) Sort {
	hashes := []uint32{decl.Hash(), operatorDecls.Hash()}
	proto := &SortSort{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("Sort"), hashes), Decl: decl, OperatorDecls: operatorDecls}
	return factory.Build(proto).(*SortSort)
}

// SortList is the Go interface backing the Gom sort SortList.
type SortList interface {
	sharedobjects.Term
	isSortList()
}

// ConcSortSortList is the term type for the alternative `ConcSort(...)` of sort SortList.
type ConcSortSortList struct {
	Slots []Sort
	hash  uint32
}

func (*ConcSortSortList) isSortList() {}

func (t *ConcSortSortList) Hash() uint32 { return t.hash }

func (t *ConcSortSortList) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ConcSortSortList)
	if !ok {
		return false
	}
	if len(t.Slots) != len(o.Slots) {
		return false
	}
	for i := range t.Slots {
		if t.Slots[i] != o.Slots[i] {
			return false
		}
	}
	return true
}

func (t *ConcSortSortList) Duplicate() sharedobjects.Term {
	cp := append([]Sort(nil), t.Slots...)
	return &ConcSortSortList{Slots: cp, hash: t.hash}
}

func (t *ConcSortSortList) String() string {
	parts := make([]string, len(t.Slots))
	for i, v := range t.Slots {
		parts[i] = fmt.Sprintf("%v", v)
	}
	return "ConcSort" + "(" + strings.Join(parts, ",") + ")"
}

// MakeConcSort builds the canonical (shared) ConcSort term.
func MakeConcSort(args ...Sort) SortList {
	hashes := make([]uint32, 0, len(args))
	for _, a := range args {
		hashes = append(hashes, a.Hash())
	}
	proto := &ConcSortSortList{Slots: args, hash: sharedobjects.MixSymbol(sharedobjects.StringHash("ConcSort"), hashes)}
	return factory.Build(proto).(*ConcSortSortList)
}

// Decl is the Go interface backing the Gom sort Decl.
type Decl interface {
	sharedobjects.Term
	isDecl()
}

// CutModuleDecl is the term type for the alternative `CutModule(...)` of sort Decl.
type CutModuleDecl struct {
	MDecl ModuleDecl
	hash  uint32
}

func (*CutModuleDecl) isDecl() {}

func (t *CutModuleDecl) Hash() uint32 { return t.hash }

func (t *CutModuleDecl) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*CutModuleDecl)
	if !ok {
		return false
	}
	if t.MDecl != o.MDecl {
		return false
	}
	return true
}

func (t *CutModuleDecl) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *CutModuleDecl) String() string {
	return fmt.Sprintf("CutModule(%v)", t.MDecl)
}

// MakeCutModule builds the canonical (shared) CutModule term.
func MakeCutModule(mDecl ModuleDecl) Decl {
	hashes := []uint32{mDecl.Hash()}
	proto := &CutModuleDecl{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("CutModule"), hashes), MDecl: mDecl}
	return factory.Build(proto).(*CutModuleDecl)
}

// CutSortDecl is the term type for the alternative `CutSort(...)` of sort Decl.
type CutSortDecl struct {
	Sort SortDecl
	hash uint32
}

func (*CutSortDecl) isDecl() {}

func (t *CutSortDecl) Hash() uint32 { return t.hash }

func (t *CutSortDecl) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*CutSortDecl)
	if !ok {
		return false
	}
	if t.Sort != o.Sort {
		return false
	}
	return true
}

func (t *CutSortDecl) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *CutSortDecl) String() string {
	return fmt.Sprintf("CutSort(%v)", t.Sort)
}

// MakeCutSort builds the canonical (shared) CutSort term.
func MakeCutSort(sort SortDecl) Decl {
	hashes := []uint32{sort.Hash()}
	proto := &CutSortDecl{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("CutSort"), hashes), Sort: sort}
	return factory.Build(proto).(*CutSortDecl)
}

// CutOperatorDecl is the term type for the alternative `CutOperator(...)` of sort Decl.
type CutOperatorDecl struct {
	ODecl OperatorDecl
	hash  uint32
}

func (*CutOperatorDecl) isDecl() {}

func (t *CutOperatorDecl) Hash() uint32 { return t.hash }

func (t *CutOperatorDecl) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*CutOperatorDecl)
	if !ok {
		return false
	}
	if t.ODecl != o.ODecl {
		return false
	}
	return true
}

func (t *CutOperatorDecl) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *CutOperatorDecl) String() string {
	return fmt.Sprintf("CutOperator(%v)", t.ODecl)
}

// MakeCutOperator builds the canonical (shared) CutOperator term.
func MakeCutOperator(oDecl OperatorDecl) Decl {
	hashes := []uint32{oDecl.Hash()}
	proto := &CutOperatorDecl{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("CutOperator"), hashes), ODecl: oDecl}
	return factory.Build(proto).(*CutOperatorDecl)
}

// CutFutureOperatorDecl is the term type for the alternative `CutFutureOperator(...)` of sort Decl.
type CutFutureOperatorDecl struct {
	ODecl     OperatorDecl
	ConsOrNil Future
	hash      uint32
}

func (*CutFutureOperatorDecl) isDecl() {}

func (t *CutFutureOperatorDecl) Hash() uint32 { return t.hash }

func (t *CutFutureOperatorDecl) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*CutFutureOperatorDecl)
	if !ok {
		return false
	}
	if t.ODecl != o.ODecl {
		return false
	}
	if t.ConsOrNil != o.ConsOrNil {
		return false
	}
	return true
}

func (t *CutFutureOperatorDecl) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *CutFutureOperatorDecl) String() string {
	return fmt.Sprintf("CutFutureOperator(%v,%v)", t.ODecl, t.ConsOrNil)
}

// MakeCutFutureOperator builds the canonical (shared) CutFutureOperator term.
func MakeCutFutureOperator(oDecl OperatorDecl, consOrNil Future) Decl {
	hashes := []uint32{oDecl.Hash(), consOrNil.Hash()}
	proto := &CutFutureOperatorDecl{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("CutFutureOperator"), hashes), ODecl: oDecl, ConsOrNil: consOrNil}
	return factory.Build(proto).(*CutFutureOperatorDecl)
}

// Future is the Go interface backing the Gom sort Future.
type Future interface {
	sharedobjects.Term
	isFuture()
}

// FutureNilFuture is the term type for the alternative `FutureNil(...)` of sort Future.
type FutureNilFuture struct {
	hash uint32
}

func (*FutureNilFuture) isFuture() {}

func (t *FutureNilFuture) Hash() uint32 { return t.hash }

func (t *FutureNilFuture) Equivalent(other sharedobjects.Term) bool {
	_, ok := other.(*FutureNilFuture)
	return ok
}

func (t *FutureNilFuture) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *FutureNilFuture) String() string {
	return "FutureNil" + "()"
}

// MakeFutureNil builds the canonical (shared) FutureNil term.
func MakeFutureNil() Future {
	hashes := []uint32{}
	proto := &FutureNilFuture{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("FutureNil"), hashes)}
	return factory.Build(proto).(*FutureNilFuture)
}

// FutureConsFuture is the term type for the alternative `FutureCons(...)` of sort Future.
type FutureConsFuture struct {
	hash uint32
}

func (*FutureConsFuture) isFuture() {}

func (t *FutureConsFuture) Hash() uint32 { return t.hash }

func (t *FutureConsFuture) Equivalent(other sharedobjects.Term) bool {
	_, ok := other.(*FutureConsFuture)
	return ok
}

func (t *FutureConsFuture) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *FutureConsFuture) String() string {
	return "FutureCons" + "()"
}

// MakeFutureCons builds the canonical (shared) FutureCons term.
func MakeFutureCons() Future {
	hashes := []uint32{}
	proto := &FutureConsFuture{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("FutureCons"), hashes)}
	return factory.Build(proto).(*FutureConsFuture)
}

// HookDecl is the Go interface backing the Gom sort HookDecl.
type HookDecl interface {
	sharedobjects.Term
	isHookDecl()
}

// MakeHookDeclHookDecl is the term type for the alternative `MakeHookDecl(...)` of sort HookDecl.
type MakeHookDeclHookDecl struct {
	Pointcut   Decl
	SlotArgs   SlotList
	Code       Code
	HookType   HookKind
	HasTomCode bool
	hash       uint32
}

func (*MakeHookDeclHookDecl) isHookDecl() {}

func (t *MakeHookDeclHookDecl) Hash() uint32 { return t.hash }

func (t *MakeHookDeclHookDecl) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*MakeHookDeclHookDecl)
	if !ok {
		return false
	}
	if t.Pointcut != o.Pointcut {
		return false
	}
	if t.SlotArgs != o.SlotArgs {
		return false
	}
	if t.Code != o.Code {
		return false
	}
	if t.HookType != o.HookType {
		return false
	}
	if t.HasTomCode != o.HasTomCode {
		return false
	}
	return true
}

func (t *MakeHookDeclHookDecl) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *MakeHookDeclHookDecl) String() string {
	return fmt.Sprintf("MakeHookDecl(%v,%v,%v,%v,%v)", t.Pointcut, t.SlotArgs, t.Code, t.HookType, t.HasTomCode)
}

// MakeMakeHookDecl builds the canonical (shared) MakeHookDecl term.
func MakeMakeHookDecl(pointcut Decl, slotArgs SlotList, code Code, hookType HookKind, hasTomCode bool) HookDecl {
	hashes := []uint32{pointcut.Hash(), slotArgs.Hash(), code.Hash(), hookType.Hash(), sharedobjects.StringHash(fmt.Sprintf("%v", hasTomCode))}
	proto := &MakeHookDeclHookDecl{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("MakeHookDecl"), hashes), Pointcut: pointcut, SlotArgs: slotArgs, Code: code, HookType: hookType, HasTomCode: hasTomCode}
	return factory.Build(proto).(*MakeHookDeclHookDecl)
}

// BlockHookDeclHookDecl is the term type for the alternative `BlockHookDecl(...)` of sort HookDecl.
type BlockHookDeclHookDecl struct {
	Pointcut   Decl
	Code       Code
	HasTomCode bool
	hash       uint32
}

func (*BlockHookDeclHookDecl) isHookDecl() {}

func (t *BlockHookDeclHookDecl) Hash() uint32 { return t.hash }

func (t *BlockHookDeclHookDecl) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*BlockHookDeclHookDecl)
	if !ok {
		return false
	}
	if t.Pointcut != o.Pointcut {
		return false
	}
	if t.Code != o.Code {
		return false
	}
	if t.HasTomCode != o.HasTomCode {
		return false
	}
	return true
}

func (t *BlockHookDeclHookDecl) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *BlockHookDeclHookDecl) String() string {
	return fmt.Sprintf("BlockHookDecl(%v,%v,%v)", t.Pointcut, t.Code, t.HasTomCode)
}

// MakeBlockHookDecl builds the canonical (shared) BlockHookDecl term.
func MakeBlockHookDecl(pointcut Decl, code Code, hasTomCode bool) HookDecl {
	hashes := []uint32{pointcut.Hash(), code.Hash(), sharedobjects.StringHash(fmt.Sprintf("%v", hasTomCode))}
	proto := &BlockHookDeclHookDecl{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("BlockHookDecl"), hashes), Pointcut: pointcut, Code: code, HasTomCode: hasTomCode}
	return factory.Build(proto).(*BlockHookDeclHookDecl)
}

// InterfaceHookDeclHookDecl is the term type for the alternative `InterfaceHookDecl(...)` of sort HookDecl.
type InterfaceHookDeclHookDecl struct {
	Pointcut Decl
	Code     Code
	hash     uint32
}

func (*InterfaceHookDeclHookDecl) isHookDecl() {}

func (t *InterfaceHookDeclHookDecl) Hash() uint32 { return t.hash }

func (t *InterfaceHookDeclHookDecl) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*InterfaceHookDeclHookDecl)
	if !ok {
		return false
	}
	if t.Pointcut != o.Pointcut {
		return false
	}
	if t.Code != o.Code {
		return false
	}
	return true
}

func (t *InterfaceHookDeclHookDecl) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *InterfaceHookDeclHookDecl) String() string {
	return fmt.Sprintf("InterfaceHookDecl(%v,%v)", t.Pointcut, t.Code)
}

// MakeInterfaceHookDecl builds the canonical (shared) InterfaceHookDecl term.
func MakeInterfaceHookDecl(pointcut Decl, code Code) HookDecl {
	hashes := []uint32{pointcut.Hash(), code.Hash()}
	proto := &InterfaceHookDeclHookDecl{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("InterfaceHookDecl"), hashes), Pointcut: pointcut, Code: code}
	return factory.Build(proto).(*InterfaceHookDeclHookDecl)
}

// ImportHookDeclHookDecl is the term type for the alternative `ImportHookDecl(...)` of sort HookDecl.
type ImportHookDeclHookDecl struct {
	Pointcut Decl
	Code     Code
	hash     uint32
}

func (*ImportHookDeclHookDecl) isHookDecl() {}

func (t *ImportHookDeclHookDecl) Hash() uint32 { return t.hash }

func (t *ImportHookDeclHookDecl) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ImportHookDeclHookDecl)
	if !ok {
		return false
	}
	if t.Pointcut != o.Pointcut {
		return false
	}
	if t.Code != o.Code {
		return false
	}
	return true
}

func (t *ImportHookDeclHookDecl) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *ImportHookDeclHookDecl) String() string {
	return fmt.Sprintf("ImportHookDecl(%v,%v)", t.Pointcut, t.Code)
}

// MakeImportHookDecl builds the canonical (shared) ImportHookDecl term.
func MakeImportHookDecl(pointcut Decl, code Code) HookDecl {
	hashes := []uint32{pointcut.Hash(), code.Hash()}
	proto := &ImportHookDeclHookDecl{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("ImportHookDecl"), hashes), Pointcut: pointcut, Code: code}
	return factory.Build(proto).(*ImportHookDeclHookDecl)
}

// MappingHookDeclHookDecl is the term type for the alternative `MappingHookDecl(...)` of sort HookDecl.
type MappingHookDeclHookDecl struct {
	Pointcut Decl
	Code     Code
	hash     uint32
}

func (*MappingHookDeclHookDecl) isHookDecl() {}

func (t *MappingHookDeclHookDecl) Hash() uint32 { return t.hash }

func (t *MappingHookDeclHookDecl) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*MappingHookDeclHookDecl)
	if !ok {
		return false
	}
	if t.Pointcut != o.Pointcut {
		return false
	}
	if t.Code != o.Code {
		return false
	}
	return true
}

func (t *MappingHookDeclHookDecl) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *MappingHookDeclHookDecl) String() string {
	return fmt.Sprintf("MappingHookDecl(%v,%v)", t.Pointcut, t.Code)
}

// MakeMappingHookDecl builds the canonical (shared) MappingHookDecl term.
func MakeMappingHookDecl(pointcut Decl, code Code) HookDecl {
	hashes := []uint32{pointcut.Hash(), code.Hash()}
	proto := &MappingHookDeclHookDecl{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("MappingHookDecl"), hashes), Pointcut: pointcut, Code: code}
	return factory.Build(proto).(*MappingHookDeclHookDecl)
}

// HookDeclList is the Go interface backing the Gom sort HookDeclList.
type HookDeclList interface {
	sharedobjects.Term
	isHookDeclList()
}

// ConcHookDeclHookDeclList is the term type for the alternative `ConcHookDecl(...)` of sort HookDeclList.
type ConcHookDeclHookDeclList struct {
	Slots []HookDecl
	hash  uint32
}

func (*ConcHookDeclHookDeclList) isHookDeclList() {}

func (t *ConcHookDeclHookDeclList) Hash() uint32 { return t.hash }

func (t *ConcHookDeclHookDeclList) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ConcHookDeclHookDeclList)
	if !ok {
		return false
	}
	if len(t.Slots) != len(o.Slots) {
		return false
	}
	for i := range t.Slots {
		if t.Slots[i] != o.Slots[i] {
			return false
		}
	}
	return true
}

func (t *ConcHookDeclHookDeclList) Duplicate() sharedobjects.Term {
	cp := append([]HookDecl(nil), t.Slots...)
	return &ConcHookDeclHookDeclList{Slots: cp, hash: t.hash}
}

func (t *ConcHookDeclHookDeclList) String() string {
	parts := make([]string, len(t.Slots))
	for i, v := range t.Slots {
		parts[i] = fmt.Sprintf("%v", v)
	}
	return "ConcHookDecl" + "(" + strings.Join(parts, ",") + ")"
}

// MakeConcHookDecl builds the canonical (shared) ConcHookDecl term.
func MakeConcHookDecl(args ...HookDecl) HookDeclList {
	hashes := make([]uint32, 0, len(args))
	for _, a := range args {
		hashes = append(hashes, a.Hash())
	}
	proto := &ConcHookDeclHookDeclList{Slots: args, hash: sharedobjects.MixSymbol(sharedobjects.StringHash("ConcHookDecl"), hashes)}
	return factory.Build(proto).(*ConcHookDeclHookDeclList)
}

// HookContent is the Go interface backing the Gom sort HookContent.
type HookContent interface {
	sharedobjects.Term
	isHookContent()
}

// HookCodeHookContent is the term type for the alternative `HookCode(...)` of sort HookContent.
type HookCodeHookContent struct {
	StringCode string
	hash       uint32
}

func (*HookCodeHookContent) isHookContent() {}

func (t *HookCodeHookContent) Hash() uint32 { return t.hash }

func (t *HookCodeHookContent) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*HookCodeHookContent)
	if !ok {
		return false
	}
	if t.StringCode != o.StringCode {
		return false
	}
	return true
}

func (t *HookCodeHookContent) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *HookCodeHookContent) String() string {
	return fmt.Sprintf("HookCode(%s)", sharedobjects.JavaEscape(t.StringCode))
}

// MakeHookCode builds the canonical (shared) HookCode term.
func MakeHookCode(stringCode string) HookContent {
	hashes := []uint32{sharedobjects.StringHash(fmt.Sprintf("%v", stringCode))}
	proto := &HookCodeHookContent{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("HookCode"), hashes), StringCode: stringCode}
	return factory.Build(proto).(*HookCodeHookContent)
}

// HookRulesHookContent is the term type for the alternative `HookRules(...)` of sort HookContent.
type HookRulesHookContent struct {
	Rules RuleList
	hash  uint32
}

func (*HookRulesHookContent) isHookContent() {}

func (t *HookRulesHookContent) Hash() uint32 { return t.hash }

func (t *HookRulesHookContent) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*HookRulesHookContent)
	if !ok {
		return false
	}
	if t.Rules != o.Rules {
		return false
	}
	return true
}

func (t *HookRulesHookContent) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *HookRulesHookContent) String() string {
	return fmt.Sprintf("HookRules(%v)", t.Rules)
}

// MakeHookRules builds the canonical (shared) HookRules term.
func MakeHookRules(rules RuleList) HookContent {
	hashes := []uint32{rules.Hash()}
	proto := &HookRulesHookContent{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("HookRules"), hashes), Rules: rules}
	return factory.Build(proto).(*HookRulesHookContent)
}

// Pair is the Go interface backing the Gom sort Pair.
type Pair interface {
	sharedobjects.Term
	isPair()
}

// ModHookPairPair is the term type for the alternative `ModHookPair(...)` of sort Pair.
type ModHookPairPair struct {
	Modules   ModuleList
	HookDecls HookDeclList
	hash      uint32
}

func (*ModHookPairPair) isPair() {}

func (t *ModHookPairPair) Hash() uint32 { return t.hash }

func (t *ModHookPairPair) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ModHookPairPair)
	if !ok {
		return false
	}
	if t.Modules != o.Modules {
		return false
	}
	if t.HookDecls != o.HookDecls {
		return false
	}
	return true
}

func (t *ModHookPairPair) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *ModHookPairPair) String() string {
	return fmt.Sprintf("ModHookPair(%v,%v)", t.Modules, t.HookDecls)
}

// MakeModHookPair builds the canonical (shared) ModHookPair term.
func MakeModHookPair(modules ModuleList, hookDecls HookDeclList) Pair {
	hashes := []uint32{modules.Hash(), hookDecls.Hash()}
	proto := &ModHookPairPair{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("ModHookPair"), hashes), Modules: modules, HookDecls: hookDecls}
	return factory.Build(proto).(*ModHookPairPair)
}

// Code generated by tom/tomgo backend from Objects. DO NOT EDIT.
package gomast

import (
	"fmt"
	"strings"

	"tom/tomgo/stable/library/sharedobjects"
)

// underscore-prevent: tolerate unused imports if a module has no slots of these types.
var _ = fmt.Sprintf
var _ = strings.Join

// GomClass is the Go interface backing the Gom sort GomClass.
type GomClass interface {
	sharedobjects.Term
	isGomClass()
}

// AbstractTypeClassGomClass is the term type for the alternative `AbstractTypeClass(...)` of sort GomClass.
type AbstractTypeClassGomClass struct {
	ClassName ClassName
	Mapping   ClassName
	SortList  ClassNameList
	Hooks     HookList
	hash      uint32
}

func (*AbstractTypeClassGomClass) isGomClass() {}

func (t *AbstractTypeClassGomClass) Hash() uint32 { return t.hash }

func (t *AbstractTypeClassGomClass) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*AbstractTypeClassGomClass)
	if !ok {
		return false
	}
	if t.ClassName != o.ClassName {
		return false
	}
	if t.Mapping != o.Mapping {
		return false
	}
	if t.SortList != o.SortList {
		return false
	}
	if t.Hooks != o.Hooks {
		return false
	}
	return true
}

func (t *AbstractTypeClassGomClass) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *AbstractTypeClassGomClass) String() string {
	return fmt.Sprintf("AbstractTypeClass(%v,%v,%v,%v)", t.ClassName, t.Mapping, t.SortList, t.Hooks)
}

// MakeAbstractTypeClass builds the canonical (shared) AbstractTypeClass term.
func MakeAbstractTypeClass(className ClassName, mapping ClassName, sortList ClassNameList, hooks HookList) GomClass {
	hashes := []uint32{className.Hash(), mapping.Hash(), sortList.Hash(), hooks.Hash()}
	proto := &AbstractTypeClassGomClass{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("AbstractTypeClass"), hashes), ClassName: className, Mapping: mapping, SortList: sortList, Hooks: hooks}
	return factory.Build(proto).(*AbstractTypeClassGomClass)
}

// SortClassGomClass is the term type for the alternative `SortClass(...)` of sort GomClass.
type SortClassGomClass struct {
	ClassName         ClassName
	AbstractType      ClassName
	Mapping           ClassName
	Operators         ClassNameList
	VariadicOperators ClassNameList
	OperatorClasses   GomClassList
	SlotFields        SlotFieldList
	Hooks             HookList
	hash              uint32
}

func (*SortClassGomClass) isGomClass() {}

func (t *SortClassGomClass) Hash() uint32 { return t.hash }

func (t *SortClassGomClass) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*SortClassGomClass)
	if !ok {
		return false
	}
	if t.ClassName != o.ClassName {
		return false
	}
	if t.AbstractType != o.AbstractType {
		return false
	}
	if t.Mapping != o.Mapping {
		return false
	}
	if t.Operators != o.Operators {
		return false
	}
	if t.VariadicOperators != o.VariadicOperators {
		return false
	}
	if t.OperatorClasses != o.OperatorClasses {
		return false
	}
	if t.SlotFields != o.SlotFields {
		return false
	}
	if t.Hooks != o.Hooks {
		return false
	}
	return true
}

func (t *SortClassGomClass) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *SortClassGomClass) String() string {
	return fmt.Sprintf("SortClass(%v,%v,%v,%v,%v,%v,%v,%v)", t.ClassName, t.AbstractType, t.Mapping, t.Operators, t.VariadicOperators, t.OperatorClasses, t.SlotFields, t.Hooks)
}

// MakeSortClass builds the canonical (shared) SortClass term.
func MakeSortClass(className ClassName, abstractType ClassName, mapping ClassName, operators ClassNameList, variadicOperators ClassNameList, operatorClasses GomClassList, slotFields SlotFieldList, hooks HookList) GomClass {
	hashes := []uint32{className.Hash(), abstractType.Hash(), mapping.Hash(), operators.Hash(), variadicOperators.Hash(), operatorClasses.Hash(), slotFields.Hash(), hooks.Hash()}
	proto := &SortClassGomClass{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("SortClass"), hashes), ClassName: className, AbstractType: abstractType, Mapping: mapping, Operators: operators, VariadicOperators: variadicOperators, OperatorClasses: operatorClasses, SlotFields: slotFields, Hooks: hooks}
	return factory.Build(proto).(*SortClassGomClass)
}

// OperatorClassGomClass is the term type for the alternative `OperatorClass(...)` of sort GomClass.
type OperatorClassGomClass struct {
	ClassName    ClassName
	AbstractType ClassName
	ExtendsType  ClassName
	Mapping      ClassName
	SortName     ClassName
	SlotFields   SlotFieldList
	Hooks        HookList
	Comments     string
	hash         uint32
}

func (*OperatorClassGomClass) isGomClass() {}

func (t *OperatorClassGomClass) Hash() uint32 { return t.hash }

func (t *OperatorClassGomClass) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*OperatorClassGomClass)
	if !ok {
		return false
	}
	if t.ClassName != o.ClassName {
		return false
	}
	if t.AbstractType != o.AbstractType {
		return false
	}
	if t.ExtendsType != o.ExtendsType {
		return false
	}
	if t.Mapping != o.Mapping {
		return false
	}
	if t.SortName != o.SortName {
		return false
	}
	if t.SlotFields != o.SlotFields {
		return false
	}
	if t.Hooks != o.Hooks {
		return false
	}
	if t.Comments != o.Comments {
		return false
	}
	return true
}

func (t *OperatorClassGomClass) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *OperatorClassGomClass) String() string {
	return fmt.Sprintf("OperatorClass(%v,%v,%v,%v,%v,%v,%v,%q)", t.ClassName, t.AbstractType, t.ExtendsType, t.Mapping, t.SortName, t.SlotFields, t.Hooks, t.Comments)
}

// MakeOperatorClass builds the canonical (shared) OperatorClass term.
func MakeOperatorClass(className ClassName, abstractType ClassName, extendsType ClassName, mapping ClassName, sortName ClassName, slotFields SlotFieldList, hooks HookList, comments string) GomClass {
	hashes := []uint32{className.Hash(), abstractType.Hash(), extendsType.Hash(), mapping.Hash(), sortName.Hash(), slotFields.Hash(), hooks.Hash(), sharedobjects.StringHash(fmt.Sprintf("%v", comments))}
	proto := &OperatorClassGomClass{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("OperatorClass"), hashes), ClassName: className, AbstractType: abstractType, ExtendsType: extendsType, Mapping: mapping, SortName: sortName, SlotFields: slotFields, Hooks: hooks, Comments: comments}
	return factory.Build(proto).(*OperatorClassGomClass)
}

// VariadicOperatorClassGomClass is the term type for the alternative `VariadicOperatorClass(...)` of sort GomClass.
type VariadicOperatorClassGomClass struct {
	ClassName    ClassName
	AbstractType ClassName
	Mapping      ClassName
	SortName     ClassName
	Empty        GomClass
	Cons         GomClass
	Hooks        HookList
	Comments     string
	hash         uint32
}

func (*VariadicOperatorClassGomClass) isGomClass() {}

func (t *VariadicOperatorClassGomClass) Hash() uint32 { return t.hash }

func (t *VariadicOperatorClassGomClass) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*VariadicOperatorClassGomClass)
	if !ok {
		return false
	}
	if t.ClassName != o.ClassName {
		return false
	}
	if t.AbstractType != o.AbstractType {
		return false
	}
	if t.Mapping != o.Mapping {
		return false
	}
	if t.SortName != o.SortName {
		return false
	}
	if t.Empty != o.Empty {
		return false
	}
	if t.Cons != o.Cons {
		return false
	}
	if t.Hooks != o.Hooks {
		return false
	}
	if t.Comments != o.Comments {
		return false
	}
	return true
}

func (t *VariadicOperatorClassGomClass) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *VariadicOperatorClassGomClass) String() string {
	return fmt.Sprintf("VariadicOperatorClass(%v,%v,%v,%v,%v,%v,%v,%q)", t.ClassName, t.AbstractType, t.Mapping, t.SortName, t.Empty, t.Cons, t.Hooks, t.Comments)
}

// MakeVariadicOperatorClass builds the canonical (shared) VariadicOperatorClass term.
func MakeVariadicOperatorClass(className ClassName, abstractType ClassName, mapping ClassName, sortName ClassName, empty GomClass, cons GomClass, hooks HookList, comments string) GomClass {
	hashes := []uint32{className.Hash(), abstractType.Hash(), mapping.Hash(), sortName.Hash(), empty.Hash(), cons.Hash(), hooks.Hash(), sharedobjects.StringHash(fmt.Sprintf("%v", comments))}
	proto := &VariadicOperatorClassGomClass{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("VariadicOperatorClass"), hashes), ClassName: className, AbstractType: abstractType, Mapping: mapping, SortName: sortName, Empty: empty, Cons: cons, Hooks: hooks, Comments: comments}
	return factory.Build(proto).(*VariadicOperatorClassGomClass)
}

// TomMappingGomClass is the term type for the alternative `TomMapping(...)` of sort GomClass.
type TomMappingGomClass struct {
	ClassName       ClassName
	SortClasses     GomClassList
	OperatorClasses GomClassList
	hash            uint32
}

func (*TomMappingGomClass) isGomClass() {}

func (t *TomMappingGomClass) Hash() uint32 { return t.hash }

func (t *TomMappingGomClass) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*TomMappingGomClass)
	if !ok {
		return false
	}
	if t.ClassName != o.ClassName {
		return false
	}
	if t.SortClasses != o.SortClasses {
		return false
	}
	if t.OperatorClasses != o.OperatorClasses {
		return false
	}
	return true
}

func (t *TomMappingGomClass) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *TomMappingGomClass) String() string {
	return fmt.Sprintf("TomMapping(%v,%v,%v)", t.ClassName, t.SortClasses, t.OperatorClasses)
}

// MakeTomMapping builds the canonical (shared) TomMapping term.
func MakeTomMapping(className ClassName, sortClasses GomClassList, operatorClasses GomClassList) GomClass {
	hashes := []uint32{className.Hash(), sortClasses.Hash(), operatorClasses.Hash()}
	proto := &TomMappingGomClass{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("TomMapping"), hashes), ClassName: className, SortClasses: sortClasses, OperatorClasses: operatorClasses}
	return factory.Build(proto).(*TomMappingGomClass)
}

// GomClassList is the Go interface backing the Gom sort GomClassList.
type GomClassList interface {
	sharedobjects.Term
	isGomClassList()
}

// ConcGomClassGomClassList is the term type for the alternative `ConcGomClass(...)` of sort GomClassList.
type ConcGomClassGomClassList struct {
	Slots []GomClass
	hash  uint32
}

func (*ConcGomClassGomClassList) isGomClassList() {}

func (t *ConcGomClassGomClassList) Hash() uint32 { return t.hash }

func (t *ConcGomClassGomClassList) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ConcGomClassGomClassList)
	if !ok {
		return false
	}
	if len(t.Slots) != len(o.Slots) {
		return false
	}
	for i := range t.Slots {
		if t.Slots[i] != o.Slots[i] {
			return false
		}
	}
	return true
}

func (t *ConcGomClassGomClassList) Duplicate() sharedobjects.Term {
	cp := append([]GomClass(nil), t.Slots...)
	return &ConcGomClassGomClassList{Slots: cp, hash: t.hash}
}

func (t *ConcGomClassGomClassList) String() string {
	parts := make([]string, len(t.Slots))
	for i, v := range t.Slots {
		parts[i] = fmt.Sprintf("%v", v)
	}
	return "ConcGomClass" + "(" + strings.Join(parts, ",") + ")"
}

// MakeConcGomClass builds the canonical (shared) ConcGomClass term.
func MakeConcGomClass(args ...GomClass) GomClassList {
	hashes := make([]uint32, 0, len(args))
	for _, a := range args {
		hashes = append(hashes, a.Hash())
	}
	proto := &ConcGomClassGomClassList{Slots: args, hash: sharedobjects.MixSymbol(sharedobjects.StringHash("ConcGomClass"), hashes)}
	return factory.Build(proto).(*ConcGomClassGomClassList)
}

// ClassName is the Go interface backing the Gom sort ClassName.
type ClassName interface {
	sharedobjects.Term
	isClassName()
}

// ClassNameClassName is the term type for the alternative `ClassName(...)` of sort ClassName.
type ClassNameClassName struct {
	Pkg  string
	Name string
	hash uint32
}

func (*ClassNameClassName) isClassName() {}

func (t *ClassNameClassName) Hash() uint32 { return t.hash }

func (t *ClassNameClassName) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ClassNameClassName)
	if !ok {
		return false
	}
	if t.Pkg != o.Pkg {
		return false
	}
	if t.Name != o.Name {
		return false
	}
	return true
}

func (t *ClassNameClassName) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *ClassNameClassName) String() string {
	return fmt.Sprintf("ClassName(%q,%q)", t.Pkg, t.Name)
}

// MakeClassName builds the canonical (shared) ClassName term.
func MakeClassName(pkg string, name string) ClassName {
	hashes := []uint32{sharedobjects.StringHash(fmt.Sprintf("%v", pkg)), sharedobjects.StringHash(fmt.Sprintf("%v", name))}
	proto := &ClassNameClassName{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("ClassName"), hashes), Pkg: pkg, Name: name}
	return factory.Build(proto).(*ClassNameClassName)
}

// ClassNameList is the Go interface backing the Gom sort ClassNameList.
type ClassNameList interface {
	sharedobjects.Term
	isClassNameList()
}

// ConcClassNameClassNameList is the term type for the alternative `ConcClassName(...)` of sort ClassNameList.
type ConcClassNameClassNameList struct {
	Slots []ClassName
	hash  uint32
}

func (*ConcClassNameClassNameList) isClassNameList() {}

func (t *ConcClassNameClassNameList) Hash() uint32 { return t.hash }

func (t *ConcClassNameClassNameList) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ConcClassNameClassNameList)
	if !ok {
		return false
	}
	if len(t.Slots) != len(o.Slots) {
		return false
	}
	for i := range t.Slots {
		if t.Slots[i] != o.Slots[i] {
			return false
		}
	}
	return true
}

func (t *ConcClassNameClassNameList) Duplicate() sharedobjects.Term {
	cp := append([]ClassName(nil), t.Slots...)
	return &ConcClassNameClassNameList{Slots: cp, hash: t.hash}
}

func (t *ConcClassNameClassNameList) String() string {
	parts := make([]string, len(t.Slots))
	for i, v := range t.Slots {
		parts[i] = fmt.Sprintf("%v", v)
	}
	return "ConcClassName" + "(" + strings.Join(parts, ",") + ")"
}

// MakeConcClassName builds the canonical (shared) ConcClassName term.
func MakeConcClassName(args ...ClassName) ClassNameList {
	hashes := make([]uint32, 0, len(args))
	for _, a := range args {
		hashes = append(hashes, a.Hash())
	}
	proto := &ConcClassNameClassNameList{Slots: args, hash: sharedobjects.MixSymbol(sharedobjects.StringHash("ConcClassName"), hashes)}
	return factory.Build(proto).(*ConcClassNameClassNameList)
}

// SlotField is the Go interface backing the Gom sort SlotField.
type SlotField interface {
	sharedobjects.Term
	isSlotField()
}

// SlotFieldSlotField is the term type for the alternative `SlotField(...)` of sort SlotField.
type SlotFieldSlotField struct {
	Name   string
	Domain ClassName
	hash   uint32
}

func (*SlotFieldSlotField) isSlotField() {}

func (t *SlotFieldSlotField) Hash() uint32 { return t.hash }

func (t *SlotFieldSlotField) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*SlotFieldSlotField)
	if !ok {
		return false
	}
	if t.Name != o.Name {
		return false
	}
	if t.Domain != o.Domain {
		return false
	}
	return true
}

func (t *SlotFieldSlotField) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *SlotFieldSlotField) String() string {
	return fmt.Sprintf("SlotField(%q,%v)", t.Name, t.Domain)
}

// MakeSlotField builds the canonical (shared) SlotField term.
func MakeSlotField(name string, domain ClassName) SlotField {
	hashes := []uint32{sharedobjects.StringHash(fmt.Sprintf("%v", name)), domain.Hash()}
	proto := &SlotFieldSlotField{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("SlotField"), hashes), Name: name, Domain: domain}
	return factory.Build(proto).(*SlotFieldSlotField)
}

// SlotFieldList is the Go interface backing the Gom sort SlotFieldList.
type SlotFieldList interface {
	sharedobjects.Term
	isSlotFieldList()
}

// ConcSlotFieldSlotFieldList is the term type for the alternative `ConcSlotField(...)` of sort SlotFieldList.
type ConcSlotFieldSlotFieldList struct {
	Slots []SlotField
	hash  uint32
}

func (*ConcSlotFieldSlotFieldList) isSlotFieldList() {}

func (t *ConcSlotFieldSlotFieldList) Hash() uint32 { return t.hash }

func (t *ConcSlotFieldSlotFieldList) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ConcSlotFieldSlotFieldList)
	if !ok {
		return false
	}
	if len(t.Slots) != len(o.Slots) {
		return false
	}
	for i := range t.Slots {
		if t.Slots[i] != o.Slots[i] {
			return false
		}
	}
	return true
}

func (t *ConcSlotFieldSlotFieldList) Duplicate() sharedobjects.Term {
	cp := append([]SlotField(nil), t.Slots...)
	return &ConcSlotFieldSlotFieldList{Slots: cp, hash: t.hash}
}

func (t *ConcSlotFieldSlotFieldList) String() string {
	parts := make([]string, len(t.Slots))
	for i, v := range t.Slots {
		parts[i] = fmt.Sprintf("%v", v)
	}
	return "ConcSlotField" + "(" + strings.Join(parts, ",") + ")"
}

// MakeConcSlotField builds the canonical (shared) ConcSlotField term.
func MakeConcSlotField(args ...SlotField) SlotFieldList {
	hashes := make([]uint32, 0, len(args))
	for _, a := range args {
		hashes = append(hashes, a.Hash())
	}
	proto := &ConcSlotFieldSlotFieldList{Slots: args, hash: sharedobjects.MixSymbol(sharedobjects.StringHash("ConcSlotField"), hashes)}
	return factory.Build(proto).(*ConcSlotFieldSlotFieldList)
}

// Hook is the Go interface backing the Gom sort Hook.
type Hook interface {
	sharedobjects.Term
	isHook()
}

// MakeHookHook is the term type for the alternative `MakeHook(...)` of sort Hook.
type MakeHookHook struct {
	HookArguments SlotFieldList
	Code          Code
	HasTomCode    bool
	hash          uint32
}

func (*MakeHookHook) isHook() {}

func (t *MakeHookHook) Hash() uint32 { return t.hash }

func (t *MakeHookHook) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*MakeHookHook)
	if !ok {
		return false
	}
	if t.HookArguments != o.HookArguments {
		return false
	}
	if t.Code != o.Code {
		return false
	}
	if t.HasTomCode != o.HasTomCode {
		return false
	}
	return true
}

func (t *MakeHookHook) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *MakeHookHook) String() string {
	return fmt.Sprintf("MakeHook(%v,%v,%v)", t.HookArguments, t.Code, t.HasTomCode)
}

// MakeMakeHook builds the canonical (shared) MakeHook term.
func MakeMakeHook(hookArguments SlotFieldList, code Code, hasTomCode bool) Hook {
	hashes := []uint32{hookArguments.Hash(), code.Hash(), sharedobjects.StringHash(fmt.Sprintf("%v", hasTomCode))}
	proto := &MakeHookHook{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("MakeHook"), hashes), HookArguments: hookArguments, Code: code, HasTomCode: hasTomCode}
	return factory.Build(proto).(*MakeHookHook)
}

// MakeBeforeHookHook is the term type for the alternative `MakeBeforeHook(...)` of sort Hook.
type MakeBeforeHookHook struct {
	HookArguments SlotFieldList
	Code          Code
	HasTomCode    bool
	hash          uint32
}

func (*MakeBeforeHookHook) isHook() {}

func (t *MakeBeforeHookHook) Hash() uint32 { return t.hash }

func (t *MakeBeforeHookHook) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*MakeBeforeHookHook)
	if !ok {
		return false
	}
	if t.HookArguments != o.HookArguments {
		return false
	}
	if t.Code != o.Code {
		return false
	}
	if t.HasTomCode != o.HasTomCode {
		return false
	}
	return true
}

func (t *MakeBeforeHookHook) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *MakeBeforeHookHook) String() string {
	return fmt.Sprintf("MakeBeforeHook(%v,%v,%v)", t.HookArguments, t.Code, t.HasTomCode)
}

// MakeMakeBeforeHook builds the canonical (shared) MakeBeforeHook term.
func MakeMakeBeforeHook(hookArguments SlotFieldList, code Code, hasTomCode bool) Hook {
	hashes := []uint32{hookArguments.Hash(), code.Hash(), sharedobjects.StringHash(fmt.Sprintf("%v", hasTomCode))}
	proto := &MakeBeforeHookHook{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("MakeBeforeHook"), hashes), HookArguments: hookArguments, Code: code, HasTomCode: hasTomCode}
	return factory.Build(proto).(*MakeBeforeHookHook)
}

// BlockHookHook is the term type for the alternative `BlockHook(...)` of sort Hook.
type BlockHookHook struct {
	Code       Code
	HasTomCode bool
	hash       uint32
}

func (*BlockHookHook) isHook() {}

func (t *BlockHookHook) Hash() uint32 { return t.hash }

func (t *BlockHookHook) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*BlockHookHook)
	if !ok {
		return false
	}
	if t.Code != o.Code {
		return false
	}
	if t.HasTomCode != o.HasTomCode {
		return false
	}
	return true
}

func (t *BlockHookHook) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *BlockHookHook) String() string {
	return fmt.Sprintf("BlockHook(%v,%v)", t.Code, t.HasTomCode)
}

// MakeBlockHook builds the canonical (shared) BlockHook term.
func MakeBlockHook(code Code, hasTomCode bool) Hook {
	hashes := []uint32{code.Hash(), sharedobjects.StringHash(fmt.Sprintf("%v", hasTomCode))}
	proto := &BlockHookHook{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("BlockHook"), hashes), Code: code, HasTomCode: hasTomCode}
	return factory.Build(proto).(*BlockHookHook)
}

// InterfaceHookHook is the term type for the alternative `InterfaceHook(...)` of sort Hook.
type InterfaceHookHook struct {
	Code Code
	hash uint32
}

func (*InterfaceHookHook) isHook() {}

func (t *InterfaceHookHook) Hash() uint32 { return t.hash }

func (t *InterfaceHookHook) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*InterfaceHookHook)
	if !ok {
		return false
	}
	if t.Code != o.Code {
		return false
	}
	return true
}

func (t *InterfaceHookHook) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *InterfaceHookHook) String() string {
	return fmt.Sprintf("InterfaceHook(%v)", t.Code)
}

// MakeInterfaceHook builds the canonical (shared) InterfaceHook term.
func MakeInterfaceHook(code Code) Hook {
	hashes := []uint32{code.Hash()}
	proto := &InterfaceHookHook{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("InterfaceHook"), hashes), Code: code}
	return factory.Build(proto).(*InterfaceHookHook)
}

// ImportHookHook is the term type for the alternative `ImportHook(...)` of sort Hook.
type ImportHookHook struct {
	Code Code
	hash uint32
}

func (*ImportHookHook) isHook() {}

func (t *ImportHookHook) Hash() uint32 { return t.hash }

func (t *ImportHookHook) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ImportHookHook)
	if !ok {
		return false
	}
	if t.Code != o.Code {
		return false
	}
	return true
}

func (t *ImportHookHook) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *ImportHookHook) String() string {
	return fmt.Sprintf("ImportHook(%v)", t.Code)
}

// MakeImportHook builds the canonical (shared) ImportHook term.
func MakeImportHook(code Code) Hook {
	hashes := []uint32{code.Hash()}
	proto := &ImportHookHook{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("ImportHook"), hashes), Code: code}
	return factory.Build(proto).(*ImportHookHook)
}

// MappingHookHook is the term type for the alternative `MappingHook(...)` of sort Hook.
type MappingHookHook struct {
	Code Code
	hash uint32
}

func (*MappingHookHook) isHook() {}

func (t *MappingHookHook) Hash() uint32 { return t.hash }

func (t *MappingHookHook) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*MappingHookHook)
	if !ok {
		return false
	}
	if t.Code != o.Code {
		return false
	}
	return true
}

func (t *MappingHookHook) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *MappingHookHook) String() string {
	return fmt.Sprintf("MappingHook(%v)", t.Code)
}

// MakeMappingHook builds the canonical (shared) MappingHook term.
func MakeMappingHook(code Code) Hook {
	hashes := []uint32{code.Hash()}
	proto := &MappingHookHook{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("MappingHook"), hashes), Code: code}
	return factory.Build(proto).(*MappingHookHook)
}

// HookList is the Go interface backing the Gom sort HookList.
type HookList interface {
	sharedobjects.Term
	isHookList()
	ContainsTomCode() bool
}

// ConcHookHookList is the term type for the alternative `ConcHook(...)` of sort HookList.
type ConcHookHookList struct {
	Slots []Hook
	hash  uint32
}

func (*ConcHookHookList) isHookList() {}

func (t *ConcHookHookList) Hash() uint32 { return t.hash }

func (t *ConcHookHookList) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ConcHookHookList)
	if !ok {
		return false
	}
	if len(t.Slots) != len(o.Slots) {
		return false
	}
	for i := range t.Slots {
		if t.Slots[i] != o.Slots[i] {
			return false
		}
	}
	return true
}

func (t *ConcHookHookList) Duplicate() sharedobjects.Term {
	cp := append([]Hook(nil), t.Slots...)
	return &ConcHookHookList{Slots: cp, hash: t.hash}
}

func (t *ConcHookHookList) String() string {
	parts := make([]string, len(t.Slots))
	for i, v := range t.Slots {
		parts[i] = fmt.Sprintf("%v", v)
	}
	return "ConcHook" + "(" + strings.Join(parts, ",") + ")"
}

// MakeConcHook builds the canonical (shared) ConcHook term.
func MakeConcHook(args ...Hook) HookList {
	hashes := make([]uint32, 0, len(args))
	for _, a := range args {
		hashes = append(hashes, a.Hash())
	}
	proto := &ConcHookHookList{Slots: args, hash: sharedobjects.MixSymbol(sharedobjects.StringHash("ConcHook"), hashes)}
	return factory.Build(proto).(*ConcHookHookList)
}

// ContainsTomCode reports whether any child hook is a
// MakeHook/MakeBeforeHook/BlockHook flagged HasTomCode=true.
// Lowered from `sort HookList:block()` in Objects.gom.
func (t *ConcHookHookList) ContainsTomCode() bool {
	for _, h := range t.Slots {
		switch ht := h.(type) {
		case *MakeHookHook:
			if ht.HasTomCode {
				return true
			}
		case *MakeBeforeHookHook:
			if ht.HasTomCode {
				return true
			}
		case *BlockHookHook:
			if ht.HasTomCode {
				return true
			}
		}
	}
	return false
}

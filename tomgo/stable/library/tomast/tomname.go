// Code generated by tom/tomgo backend from TomName. DO NOT EDIT.
package tomast

import (
	"fmt"
	"strings"

	"tom/tomgo/stable/library/sharedobjects"
)

// underscore-prevent: tolerate unused imports if a module has no slots of these types.
var _ = fmt.Sprintf
var _ = strings.Join

// TomName is the Go interface backing the Gom sort TomName.
type TomName interface {
	sharedobjects.Term
	isTomName()
}

// NameTomName is the term type for the alternative `Name(...)` of sort TomName.
type NameTomName struct {
	String_ string
	hash    uint32
}

func (*NameTomName) isTomName() {}

func (t *NameTomName) Hash() uint32 { return t.hash }

func (t *NameTomName) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*NameTomName)
	if !ok {
		return false
	}
	if t.String_ != o.String_ {
		return false
	}
	return true
}

func (t *NameTomName) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *NameTomName) String() string {
	return fmt.Sprintf("Name(%s)", sharedobjects.JavaEscape(t.String_))
}

// MakeName builds the canonical (shared) Name term.
func MakeName(string_ string) TomName {
	hashes := []uint32{sharedobjects.StringHash(fmt.Sprintf("%v", string_))}
	proto := &NameTomName{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("Name"), hashes), String_: string_}
	return factory.Build(proto).(*NameTomName)
}

// PositionNameTomName is the term type for the alternative `PositionName(...)` of sort TomName.
type PositionNameTomName struct {
	NumberList TomNumberList
	hash       uint32
}

func (*PositionNameTomName) isTomName() {}

func (t *PositionNameTomName) Hash() uint32 { return t.hash }

func (t *PositionNameTomName) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*PositionNameTomName)
	if !ok {
		return false
	}
	if t.NumberList != o.NumberList {
		return false
	}
	return true
}

func (t *PositionNameTomName) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *PositionNameTomName) String() string {
	return fmt.Sprintf("PositionName(%v)", t.NumberList)
}

// MakePositionName builds the canonical (shared) PositionName term.
func MakePositionName(numberList TomNumberList) TomName {
	hashes := []uint32{numberList.Hash()}
	proto := &PositionNameTomName{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("PositionName"), hashes), NumberList: numberList}
	return factory.Build(proto).(*PositionNameTomName)
}

// EmptyNameTomName is the term type for the alternative `EmptyName(...)` of sort TomName.
type EmptyNameTomName struct {
	hash uint32
}

func (*EmptyNameTomName) isTomName() {}

func (t *EmptyNameTomName) Hash() uint32 { return t.hash }

func (t *EmptyNameTomName) Equivalent(other sharedobjects.Term) bool {
	_, ok := other.(*EmptyNameTomName)
	return ok
}

func (t *EmptyNameTomName) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *EmptyNameTomName) String() string {
	return "EmptyName" + "()"
}

// MakeEmptyName builds the canonical (shared) EmptyName term.
func MakeEmptyName() TomName {
	hashes := []uint32{}
	proto := &EmptyNameTomName{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("EmptyName"), hashes)}
	return factory.Build(proto).(*EmptyNameTomName)
}

// AntiNameTomName is the term type for the alternative `AntiName(...)` of sort TomName.
type AntiNameTomName struct {
	Name TomName
	hash uint32
}

func (*AntiNameTomName) isTomName() {}

func (t *AntiNameTomName) Hash() uint32 { return t.hash }

func (t *AntiNameTomName) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*AntiNameTomName)
	if !ok {
		return false
	}
	if t.Name != o.Name {
		return false
	}
	return true
}

func (t *AntiNameTomName) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *AntiNameTomName) String() string {
	return fmt.Sprintf("AntiName(%v)", t.Name)
}

// MakeAntiName builds the canonical (shared) AntiName term.
func MakeAntiName(name TomName) TomName {
	hashes := []uint32{name.Hash()}
	proto := &AntiNameTomName{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("AntiName"), hashes), Name: name}
	return factory.Build(proto).(*AntiNameTomName)
}

// TomNumber is the Go interface backing the Gom sort TomNumber.
type TomNumber interface {
	sharedobjects.Term
	isTomNumber()
}

// MatchNumberTomNumber is the term type for the alternative `MatchNumber(...)` of sort TomNumber.
type MatchNumberTomNumber struct {
	Integer int64
	hash    uint32
}

func (*MatchNumberTomNumber) isTomNumber() {}

func (t *MatchNumberTomNumber) Hash() uint32 { return t.hash }

func (t *MatchNumberTomNumber) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*MatchNumberTomNumber)
	if !ok {
		return false
	}
	if t.Integer != o.Integer {
		return false
	}
	return true
}

func (t *MatchNumberTomNumber) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *MatchNumberTomNumber) String() string {
	return fmt.Sprintf("MatchNumber(%v)", t.Integer)
}

// MakeMatchNumber builds the canonical (shared) MatchNumber term.
func MakeMatchNumber(integer int64) TomNumber {
	hashes := []uint32{sharedobjects.StringHash(fmt.Sprintf("%v", integer))}
	proto := &MatchNumberTomNumber{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("MatchNumber"), hashes), Integer: integer}
	return factory.Build(proto).(*MatchNumberTomNumber)
}

// PatternNumberTomNumber is the term type for the alternative `PatternNumber(...)` of sort TomNumber.
type PatternNumberTomNumber struct {
	Integer int64
	hash    uint32
}

func (*PatternNumberTomNumber) isTomNumber() {}

func (t *PatternNumberTomNumber) Hash() uint32 { return t.hash }

func (t *PatternNumberTomNumber) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*PatternNumberTomNumber)
	if !ok {
		return false
	}
	if t.Integer != o.Integer {
		return false
	}
	return true
}

func (t *PatternNumberTomNumber) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *PatternNumberTomNumber) String() string {
	return fmt.Sprintf("PatternNumber(%v)", t.Integer)
}

// MakePatternNumber builds the canonical (shared) PatternNumber term.
func MakePatternNumber(integer int64) TomNumber {
	hashes := []uint32{sharedobjects.StringHash(fmt.Sprintf("%v", integer))}
	proto := &PatternNumberTomNumber{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("PatternNumber"), hashes), Integer: integer}
	return factory.Build(proto).(*PatternNumberTomNumber)
}

// ListNumberTomNumber is the term type for the alternative `ListNumber(...)` of sort TomNumber.
type ListNumberTomNumber struct {
	Integer int64
	hash    uint32
}

func (*ListNumberTomNumber) isTomNumber() {}

func (t *ListNumberTomNumber) Hash() uint32 { return t.hash }

func (t *ListNumberTomNumber) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ListNumberTomNumber)
	if !ok {
		return false
	}
	if t.Integer != o.Integer {
		return false
	}
	return true
}

func (t *ListNumberTomNumber) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *ListNumberTomNumber) String() string {
	return fmt.Sprintf("ListNumber(%v)", t.Integer)
}

// MakeListNumber builds the canonical (shared) ListNumber term.
func MakeListNumber(integer int64) TomNumber {
	hashes := []uint32{sharedobjects.StringHash(fmt.Sprintf("%v", integer))}
	proto := &ListNumberTomNumber{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("ListNumber"), hashes), Integer: integer}
	return factory.Build(proto).(*ListNumberTomNumber)
}

// IndexNumberTomNumber is the term type for the alternative `IndexNumber(...)` of sort TomNumber.
type IndexNumberTomNumber struct {
	Integer int64
	hash    uint32
}

func (*IndexNumberTomNumber) isTomNumber() {}

func (t *IndexNumberTomNumber) Hash() uint32 { return t.hash }

func (t *IndexNumberTomNumber) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*IndexNumberTomNumber)
	if !ok {
		return false
	}
	if t.Integer != o.Integer {
		return false
	}
	return true
}

func (t *IndexNumberTomNumber) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *IndexNumberTomNumber) String() string {
	return fmt.Sprintf("IndexNumber(%v)", t.Integer)
}

// MakeIndexNumber builds the canonical (shared) IndexNumber term.
func MakeIndexNumber(integer int64) TomNumber {
	hashes := []uint32{sharedobjects.StringHash(fmt.Sprintf("%v", integer))}
	proto := &IndexNumberTomNumber{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("IndexNumber"), hashes), Integer: integer}
	return factory.Build(proto).(*IndexNumberTomNumber)
}

// BeginTomNumber is the term type for the alternative `Begin(...)` of sort TomNumber.
type BeginTomNumber struct {
	Integer int64
	hash    uint32
}

func (*BeginTomNumber) isTomNumber() {}

func (t *BeginTomNumber) Hash() uint32 { return t.hash }

func (t *BeginTomNumber) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*BeginTomNumber)
	if !ok {
		return false
	}
	if t.Integer != o.Integer {
		return false
	}
	return true
}

func (t *BeginTomNumber) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *BeginTomNumber) String() string {
	return fmt.Sprintf("Begin(%v)", t.Integer)
}

// MakeBegin builds the canonical (shared) Begin term.
func MakeBegin(integer int64) TomNumber {
	hashes := []uint32{sharedobjects.StringHash(fmt.Sprintf("%v", integer))}
	proto := &BeginTomNumber{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("Begin"), hashes), Integer: integer}
	return factory.Build(proto).(*BeginTomNumber)
}

// EndTomNumber is the term type for the alternative `End(...)` of sort TomNumber.
type EndTomNumber struct {
	Integer int64
	hash    uint32
}

func (*EndTomNumber) isTomNumber() {}

func (t *EndTomNumber) Hash() uint32 { return t.hash }

func (t *EndTomNumber) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*EndTomNumber)
	if !ok {
		return false
	}
	if t.Integer != o.Integer {
		return false
	}
	return true
}

func (t *EndTomNumber) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *EndTomNumber) String() string {
	return fmt.Sprintf("End(%v)", t.Integer)
}

// MakeEnd builds the canonical (shared) End term.
func MakeEnd(integer int64) TomNumber {
	hashes := []uint32{sharedobjects.StringHash(fmt.Sprintf("%v", integer))}
	proto := &EndTomNumber{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("End"), hashes), Integer: integer}
	return factory.Build(proto).(*EndTomNumber)
}

// SaveTomNumber is the term type for the alternative `Save(...)` of sort TomNumber.
type SaveTomNumber struct {
	Integer int64
	hash    uint32
}

func (*SaveTomNumber) isTomNumber() {}

func (t *SaveTomNumber) Hash() uint32 { return t.hash }

func (t *SaveTomNumber) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*SaveTomNumber)
	if !ok {
		return false
	}
	if t.Integer != o.Integer {
		return false
	}
	return true
}

func (t *SaveTomNumber) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *SaveTomNumber) String() string {
	return fmt.Sprintf("Save(%v)", t.Integer)
}

// MakeSave builds the canonical (shared) Save term.
func MakeSave(integer int64) TomNumber {
	hashes := []uint32{sharedobjects.StringHash(fmt.Sprintf("%v", integer))}
	proto := &SaveTomNumber{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("Save"), hashes), Integer: integer}
	return factory.Build(proto).(*SaveTomNumber)
}

// PositionTomNumber is the term type for the alternative `Position(...)` of sort TomNumber.
type PositionTomNumber struct {
	Integer int64
	hash    uint32
}

func (*PositionTomNumber) isTomNumber() {}

func (t *PositionTomNumber) Hash() uint32 { return t.hash }

func (t *PositionTomNumber) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*PositionTomNumber)
	if !ok {
		return false
	}
	if t.Integer != o.Integer {
		return false
	}
	return true
}

func (t *PositionTomNumber) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *PositionTomNumber) String() string {
	return fmt.Sprintf("Position(%v)", t.Integer)
}

// MakePosition builds the canonical (shared) Position term.
func MakePosition(integer int64) TomNumber {
	hashes := []uint32{sharedobjects.StringHash(fmt.Sprintf("%v", integer))}
	proto := &PositionTomNumber{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("Position"), hashes), Integer: integer}
	return factory.Build(proto).(*PositionTomNumber)
}

// AbsVarTomNumber is the term type for the alternative `AbsVar(...)` of sort TomNumber.
type AbsVarTomNumber struct {
	Integer int64
	hash    uint32
}

func (*AbsVarTomNumber) isTomNumber() {}

func (t *AbsVarTomNumber) Hash() uint32 { return t.hash }

func (t *AbsVarTomNumber) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*AbsVarTomNumber)
	if !ok {
		return false
	}
	if t.Integer != o.Integer {
		return false
	}
	return true
}

func (t *AbsVarTomNumber) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *AbsVarTomNumber) String() string {
	return fmt.Sprintf("AbsVar(%v)", t.Integer)
}

// MakeAbsVar builds the canonical (shared) AbsVar term.
func MakeAbsVar(integer int64) TomNumber {
	hashes := []uint32{sharedobjects.StringHash(fmt.Sprintf("%v", integer))}
	proto := &AbsVarTomNumber{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("AbsVar"), hashes), Integer: integer}
	return factory.Build(proto).(*AbsVarTomNumber)
}

// RenamedVarTomNumber is the term type for the alternative `RenamedVar(...)` of sort TomNumber.
type RenamedVarTomNumber struct {
	AstName TomName
	hash    uint32
}

func (*RenamedVarTomNumber) isTomNumber() {}

func (t *RenamedVarTomNumber) Hash() uint32 { return t.hash }

func (t *RenamedVarTomNumber) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*RenamedVarTomNumber)
	if !ok {
		return false
	}
	if t.AstName != o.AstName {
		return false
	}
	return true
}

func (t *RenamedVarTomNumber) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *RenamedVarTomNumber) String() string {
	return fmt.Sprintf("RenamedVar(%v)", t.AstName)
}

// MakeRenamedVar builds the canonical (shared) RenamedVar term.
func MakeRenamedVar(astName TomName) TomNumber {
	hashes := []uint32{astName.Hash()}
	proto := &RenamedVarTomNumber{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("RenamedVar"), hashes), AstName: astName}
	return factory.Build(proto).(*RenamedVarTomNumber)
}

// NameNumberTomNumber is the term type for the alternative `NameNumber(...)` of sort TomNumber.
type NameNumberTomNumber struct {
	AstName TomName
	hash    uint32
}

func (*NameNumberTomNumber) isTomNumber() {}

func (t *NameNumberTomNumber) Hash() uint32 { return t.hash }

func (t *NameNumberTomNumber) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*NameNumberTomNumber)
	if !ok {
		return false
	}
	if t.AstName != o.AstName {
		return false
	}
	return true
}

func (t *NameNumberTomNumber) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *NameNumberTomNumber) String() string {
	return fmt.Sprintf("NameNumber(%v)", t.AstName)
}

// MakeNameNumber builds the canonical (shared) NameNumber term.
func MakeNameNumber(astName TomName) TomNumber {
	// NameNumber:make hook (TomName.gom): when astName is
	// PositionName(concTomNumber(p@Position[])), return p.
	if pn, ok := astName.(*PositionNameTomName); ok {
		if list, ok := pn.NumberList.(*ConcTomNumberTomNumberList); ok && len(list.Slots) == 1 {
			if _, ok := list.Slots[0].(*PositionTomNumber); ok {
				return list.Slots[0]
			}
		}
	}
	hashes := []uint32{astName.Hash()}
	proto := &NameNumberTomNumber{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("NameNumber"), hashes), AstName: astName}
	return factory.Build(proto).(*NameNumberTomNumber)
}

// TomNumberList is the Go interface backing the Gom sort TomNumberList.
type TomNumberList interface {
	sharedobjects.Term
	isTomNumberList()
}

// ConcTomNumberTomNumberList is the term type for the alternative `concTomNumber(...)` of sort TomNumberList.
type ConcTomNumberTomNumberList struct {
	Slots []TomNumber
	hash  uint32
}

func (*ConcTomNumberTomNumberList) isTomNumberList() {}

func (t *ConcTomNumberTomNumberList) Hash() uint32 { return t.hash }

func (t *ConcTomNumberTomNumberList) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ConcTomNumberTomNumberList)
	if !ok {
		return false
	}
	if len(t.Slots) != len(o.Slots) {
		return false
	}
	for i := range t.Slots {
		if t.Slots[i] != o.Slots[i] {
			return false
		}
	}
	return true
}

func (t *ConcTomNumberTomNumberList) Duplicate() sharedobjects.Term {
	cp := append([]TomNumber(nil), t.Slots...)
	return &ConcTomNumberTomNumberList{Slots: cp, hash: t.hash}
}

func (t *ConcTomNumberTomNumberList) String() string {
	parts := make([]string, len(t.Slots))
	for i, v := range t.Slots {
		parts[i] = fmt.Sprintf("%v", v)
	}
	return "concTomNumber" + "(" + strings.Join(parts, ",") + ")"
}

// MakeConcTomNumber builds the canonical (shared) concTomNumber term.
func MakeConcTomNumber(args ...TomNumber) TomNumberList {
	// concTomNumber:make_insert hook (TomName.gom): splice the children of
	// NameNumber(PositionName(concTomNumber(p*))) into the surrounding list.
	{
		var flat []TomNumber
		for _, e := range args {
			if nn, ok := e.(*NameNumberTomNumber); ok {
				if pn, ok := nn.AstName.(*PositionNameTomName); ok {
					if inner, ok := pn.NumberList.(*ConcTomNumberTomNumberList); ok {
						flat = append(flat, inner.Slots...)
						continue
					}
				}
			}
			flat = append(flat, e)
		}
		args = flat
	}
	hashes := make([]uint32, 0, len(args))
	for _, a := range args {
		hashes = append(hashes, a.Hash())
	}
	proto := &ConcTomNumberTomNumberList{Slots: args, hash: sharedobjects.MixSymbol(sharedobjects.StringHash("concTomNumber"), hashes)}
	return factory.Build(proto).(*ConcTomNumberTomNumberList)
}

// TomNameList is the Go interface backing the Gom sort TomNameList.
type TomNameList interface {
	sharedobjects.Term
	isTomNameList()
}

// ConcTomNameTomNameList is the term type for the alternative `concTomName(...)` of sort TomNameList.
type ConcTomNameTomNameList struct {
	Slots []TomName
	hash  uint32
}

func (*ConcTomNameTomNameList) isTomNameList() {}

func (t *ConcTomNameTomNameList) Hash() uint32 { return t.hash }

func (t *ConcTomNameTomNameList) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ConcTomNameTomNameList)
	if !ok {
		return false
	}
	if len(t.Slots) != len(o.Slots) {
		return false
	}
	for i := range t.Slots {
		if t.Slots[i] != o.Slots[i] {
			return false
		}
	}
	return true
}

func (t *ConcTomNameTomNameList) Duplicate() sharedobjects.Term {
	cp := append([]TomName(nil), t.Slots...)
	return &ConcTomNameTomNameList{Slots: cp, hash: t.hash}
}

func (t *ConcTomNameTomNameList) String() string {
	parts := make([]string, len(t.Slots))
	for i, v := range t.Slots {
		parts[i] = fmt.Sprintf("%v", v)
	}
	return "concTomName" + "(" + strings.Join(parts, ",") + ")"
}

// MakeConcTomName builds the canonical (shared) concTomName term.
func MakeConcTomName(args ...TomName) TomNameList {
	hashes := make([]uint32, 0, len(args))
	for _, a := range args {
		hashes = append(hashes, a.Hash())
	}
	proto := &ConcTomNameTomNameList{Slots: args, hash: sharedobjects.MixSymbol(sharedobjects.StringHash("concTomName"), hashes)}
	return factory.Build(proto).(*ConcTomNameTomNameList)
}

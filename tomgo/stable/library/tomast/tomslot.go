// Code generated by tom/tomgo backend from TomSlot. DO NOT EDIT.
package tomast

import (
	"fmt"
	"strings"

	"tom/tomgo/stable/library/sharedobjects"
)

// underscore-prevent: tolerate unused imports if a module has no slots of these types.
var _ = fmt.Sprintf
var _ = strings.Join

// Slot is the Go interface backing the Gom sort Slot.
type Slot interface {
	sharedobjects.Term
	isSlot()
}

// PairSlotApplSlot is the term type for the alternative `PairSlotAppl(...)` of sort Slot.
type PairSlotApplSlot struct {
	SlotName TomName
	Appl     TomTerm
	hash     uint32
}

func (*PairSlotApplSlot) isSlot() {}

func (t *PairSlotApplSlot) Hash() uint32 { return t.hash }

func (t *PairSlotApplSlot) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*PairSlotApplSlot)
	if !ok {
		return false
	}
	if t.SlotName != o.SlotName {
		return false
	}
	if t.Appl != o.Appl {
		return false
	}
	return true
}

func (t *PairSlotApplSlot) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *PairSlotApplSlot) String() string {
	return fmt.Sprintf("PairSlotAppl(%v,%v)", t.SlotName, t.Appl)
}

// MakePairSlotAppl builds the canonical (shared) PairSlotAppl term.
func MakePairSlotAppl(slotName TomName, appl TomTerm) Slot {
	hashes := []uint32{slotName.Hash(), appl.Hash()}
	proto := &PairSlotApplSlot{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("PairSlotAppl"), hashes), SlotName: slotName, Appl: appl}
	return factory.Build(proto).(*PairSlotApplSlot)
}

// SlotList is the Go interface backing the Gom sort SlotList.
type SlotList interface {
	sharedobjects.Term
	isSlotList()
}

// ConcSlotSlotList is the term type for the alternative `concSlot(...)` of sort SlotList.
type ConcSlotSlotList struct {
	Slots []Slot
	hash  uint32
}

func (*ConcSlotSlotList) isSlotList() {}

func (t *ConcSlotSlotList) Hash() uint32 { return t.hash }

func (t *ConcSlotSlotList) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ConcSlotSlotList)
	if !ok {
		return false
	}
	if len(t.Slots) != len(o.Slots) {
		return false
	}
	for i := range t.Slots {
		if t.Slots[i] != o.Slots[i] {
			return false
		}
	}
	return true
}

func (t *ConcSlotSlotList) Duplicate() sharedobjects.Term {
	cp := append([]Slot(nil), t.Slots...)
	return &ConcSlotSlotList{Slots: cp, hash: t.hash}
}

func (t *ConcSlotSlotList) String() string {
	parts := make([]string, len(t.Slots))
	for i, v := range t.Slots {
		parts[i] = fmt.Sprintf("%v", v)
	}
	return "concSlot" + "(" + strings.Join(parts, ",") + ")"
}

// MakeConcSlot builds the canonical (shared) concSlot term.
func MakeConcSlot(args ...Slot) SlotList {
	hashes := make([]uint32, 0, len(args))
	for _, a := range args {
		hashes = append(hashes, a.Hash())
	}
	proto := &ConcSlotSlotList{Slots: args, hash: sharedobjects.MixSymbol(sharedobjects.StringHash("concSlot"), hashes)}
	return factory.Build(proto).(*ConcSlotSlotList)
}

// BQSlot is the Go interface backing the Gom sort BQSlot.
type BQSlot interface {
	sharedobjects.Term
	isBQSlot()
}

// PairSlotBQTermBQSlot is the term type for the alternative `PairSlotBQTerm(...)` of sort BQSlot.
type PairSlotBQTermBQSlot struct {
	SlotName TomName
	Bqterm   BQTerm
	hash     uint32
}

func (*PairSlotBQTermBQSlot) isBQSlot() {}

func (t *PairSlotBQTermBQSlot) Hash() uint32 { return t.hash }

func (t *PairSlotBQTermBQSlot) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*PairSlotBQTermBQSlot)
	if !ok {
		return false
	}
	if t.SlotName != o.SlotName {
		return false
	}
	if t.Bqterm != o.Bqterm {
		return false
	}
	return true
}

func (t *PairSlotBQTermBQSlot) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *PairSlotBQTermBQSlot) String() string {
	return fmt.Sprintf("PairSlotBQTerm(%v,%v)", t.SlotName, t.Bqterm)
}

// MakePairSlotBQTerm builds the canonical (shared) PairSlotBQTerm term.
func MakePairSlotBQTerm(slotName TomName, bqterm BQTerm) BQSlot {
	hashes := []uint32{slotName.Hash(), bqterm.Hash()}
	proto := &PairSlotBQTermBQSlot{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("PairSlotBQTerm"), hashes), SlotName: slotName, Bqterm: bqterm}
	return factory.Build(proto).(*PairSlotBQTermBQSlot)
}

// BQSlotList is the Go interface backing the Gom sort BQSlotList.
type BQSlotList interface {
	sharedobjects.Term
	isBQSlotList()
}

// ConcBQSlotBQSlotList is the term type for the alternative `concBQSlot(...)` of sort BQSlotList.
type ConcBQSlotBQSlotList struct {
	Slots []BQSlot
	hash  uint32
}

func (*ConcBQSlotBQSlotList) isBQSlotList() {}

func (t *ConcBQSlotBQSlotList) Hash() uint32 { return t.hash }

func (t *ConcBQSlotBQSlotList) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ConcBQSlotBQSlotList)
	if !ok {
		return false
	}
	if len(t.Slots) != len(o.Slots) {
		return false
	}
	for i := range t.Slots {
		if t.Slots[i] != o.Slots[i] {
			return false
		}
	}
	return true
}

func (t *ConcBQSlotBQSlotList) Duplicate() sharedobjects.Term {
	cp := append([]BQSlot(nil), t.Slots...)
	return &ConcBQSlotBQSlotList{Slots: cp, hash: t.hash}
}

func (t *ConcBQSlotBQSlotList) String() string {
	parts := make([]string, len(t.Slots))
	for i, v := range t.Slots {
		parts[i] = fmt.Sprintf("%v", v)
	}
	return "concBQSlot" + "(" + strings.Join(parts, ",") + ")"
}

// MakeConcBQSlot builds the canonical (shared) concBQSlot term.
func MakeConcBQSlot(args ...BQSlot) BQSlotList {
	hashes := make([]uint32, 0, len(args))
	for _, a := range args {
		hashes = append(hashes, a.Hash())
	}
	proto := &ConcBQSlotBQSlotList{Slots: args, hash: sharedobjects.MixSymbol(sharedobjects.StringHash("concBQSlot"), hashes)}
	return factory.Build(proto).(*ConcBQSlotBQSlotList)
}

// PairNameDecl is the Go interface backing the Gom sort PairNameDecl.
type PairNameDecl interface {
	sharedobjects.Term
	isPairNameDecl()
}

// PairNameDeclPairNameDecl is the term type for the alternative `PairNameDecl(...)` of sort PairNameDecl.
type PairNameDeclPairNameDecl struct {
	SlotName TomName
	SlotDecl Declaration
	hash     uint32
}

func (*PairNameDeclPairNameDecl) isPairNameDecl() {}

func (t *PairNameDeclPairNameDecl) Hash() uint32 { return t.hash }

func (t *PairNameDeclPairNameDecl) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*PairNameDeclPairNameDecl)
	if !ok {
		return false
	}
	if t.SlotName != o.SlotName {
		return false
	}
	if t.SlotDecl != o.SlotDecl {
		return false
	}
	return true
}

func (t *PairNameDeclPairNameDecl) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *PairNameDeclPairNameDecl) String() string {
	return fmt.Sprintf("PairNameDecl(%v,%v)", t.SlotName, t.SlotDecl)
}

// MakePairNameDecl builds the canonical (shared) PairNameDecl term.
func MakePairNameDecl(slotName TomName, slotDecl Declaration) PairNameDecl {
	hashes := []uint32{slotName.Hash(), slotDecl.Hash()}
	proto := &PairNameDeclPairNameDecl{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("PairNameDecl"), hashes), SlotName: slotName, SlotDecl: slotDecl}
	return factory.Build(proto).(*PairNameDeclPairNameDecl)
}

// PairNameDeclList is the Go interface backing the Gom sort PairNameDeclList.
type PairNameDeclList interface {
	sharedobjects.Term
	isPairNameDeclList()
}

// ConcPairNameDeclPairNameDeclList is the term type for the alternative `concPairNameDecl(...)` of sort PairNameDeclList.
type ConcPairNameDeclPairNameDeclList struct {
	Slots []PairNameDecl
	hash  uint32
}

func (*ConcPairNameDeclPairNameDeclList) isPairNameDeclList() {}

func (t *ConcPairNameDeclPairNameDeclList) Hash() uint32 { return t.hash }

func (t *ConcPairNameDeclPairNameDeclList) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*ConcPairNameDeclPairNameDeclList)
	if !ok {
		return false
	}
	if len(t.Slots) != len(o.Slots) {
		return false
	}
	for i := range t.Slots {
		if t.Slots[i] != o.Slots[i] {
			return false
		}
	}
	return true
}

func (t *ConcPairNameDeclPairNameDeclList) Duplicate() sharedobjects.Term {
	cp := append([]PairNameDecl(nil), t.Slots...)
	return &ConcPairNameDeclPairNameDeclList{Slots: cp, hash: t.hash}
}

func (t *ConcPairNameDeclPairNameDeclList) String() string {
	parts := make([]string, len(t.Slots))
	for i, v := range t.Slots {
		parts[i] = fmt.Sprintf("%v", v)
	}
	return "concPairNameDecl" + "(" + strings.Join(parts, ",") + ")"
}

// MakeConcPairNameDecl builds the canonical (shared) concPairNameDecl term.
func MakeConcPairNameDecl(args ...PairNameDecl) PairNameDeclList {
	hashes := make([]uint32, 0, len(args))
	for _, a := range args {
		hashes = append(hashes, a.Hash())
	}
	proto := &ConcPairNameDeclPairNameDeclList{Slots: args, hash: sharedobjects.MixSymbol(sharedobjects.StringHash("concPairNameDecl"), hashes)}
	return factory.Build(proto).(*ConcPairNameDeclPairNameDeclList)
}

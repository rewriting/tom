// Code generated by tom/tomgo backend from Minimal. DO NOT EDIT.
package minimal

import (
	"fmt"
	"strings"

	"tom/tomgo/stable/library/sharedobjects"
	sl "tom/tomgo/stable/library/sl"
)

// underscore-prevent: tolerate unused imports if a module has no slots of these types.
var _ = fmt.Sprintf
var _ = strings.Join
var _ sl.Strategy = nil

// factory is the per-package shared-objects factory backing every constructor.
var factory = sharedobjects.NewFactory()

// Factory exposes the underlying shared-objects factory for tests/stats.
func Factory() *sharedobjects.Factory { return factory }

// Nop is the Go interface backing the Gom sort Nop.
type Nop interface {
	sharedobjects.Term
	isNop()
}

// EmptyNopNop is the term type for the alternative `EmptyNop(...)` of sort Nop.
type EmptyNopNop struct {
	hash uint32
}

func (*EmptyNopNop) isNop() {}

func (t *EmptyNopNop) Hash() uint32 { return t.hash }

func (t *EmptyNopNop) Equivalent(other sharedobjects.Term) bool {
	_, ok := other.(*EmptyNopNop)
	return ok
}

func (t *EmptyNopNop) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *EmptyNopNop) String() string {
	return "EmptyNop" + "()"
}

func (t *EmptyNopNop) ChildCount() int { return 0 }

func (t *EmptyNopNop) ChildAt(i int) any {
	panic(fmt.Sprintf("EmptyNopNop.ChildAt: index %d out of [0,0)", i))
}

func (t *EmptyNopNop) SetChildAt(i int, child any) any {
	panic(fmt.Sprintf("EmptyNopNop.SetChildAt: index %d out of [0,0)", i))
}

func (t *EmptyNopNop) Children() []any { return nil }

func (t *EmptyNopNop) SetChildren(children []any) any { return t }

// MakeEmptyNop builds the canonical (shared) EmptyNop term.
func MakeEmptyNop() Nop {
	hashes := []uint32{}
	proto := &EmptyNopNop{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("EmptyNop"), hashes)}
	return factory.Build(proto).(*EmptyNopNop)
}

// IsEmptyNop is the `Is_EmptyNop` predicate strategy: succeeds (returns subject
// unchanged) when subject has the `EmptyNop` shape, otherwise fails with
// sl.ErrVisitFailure.
type IsEmptyNop struct{}

func (IsEmptyNop) VisitLight(subject any, _ sl.Introspector) (any, error) {
	if _, ok := subject.(*EmptyNopNop); ok {
		return subject, nil
	}
	return subject, sl.ErrVisitFailure
}
func (IsEmptyNop) ChildCount() int             { return 0 }
func (IsEmptyNop) ChildAt(int) sl.Strategy     { panic("IsEmptyNop: no children") }
func (IsEmptyNop) SetChildAt(int, sl.Strategy) { panic("IsEmptyNop: no children") }

// VisitEmptyNop is the `_EmptyNop` slot-visit strategy: when subject is `EmptyNop`,
// applies each constituent strategy to the matching child slot and
// rebuilds the term iff at least one child changed. Fails with
// sl.ErrVisitFailure when subject isn't `EmptyNop`.
type VisitEmptyNop struct {
	args []sl.Strategy
}

// NewVisitEmptyNop builds the slot-visit strategy with the supplied per-slot
// sub-strategies.
func NewVisitEmptyNop(args ...sl.Strategy) *VisitEmptyNop {
	return &VisitEmptyNop{args: args}
}

func (s *VisitEmptyNop) VisitLight(subject any, intro sl.Introspector) (any, error) {
	if _, ok := subject.(*EmptyNopNop); !ok {
		return subject, sl.ErrVisitFailure
	}
	// Nullary alt: no children to visit.
	return subject, nil
}

func (s *VisitEmptyNop) ChildCount() int                 { return len(s.args) }
func (s *VisitEmptyNop) ChildAt(i int) sl.Strategy       { return s.args[i] }
func (s *VisitEmptyNop) SetChildAt(i int, v sl.Strategy) { s.args[i] = v }

// UnaryNopNop is the term type for the alternative `UnaryNop(...)` of sort Nop.
type UnaryNopNop struct {
	Slot Nop
	hash uint32
}

func (*UnaryNopNop) isNop() {}

func (t *UnaryNopNop) Hash() uint32 { return t.hash }

func (t *UnaryNopNop) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*UnaryNopNop)
	if !ok {
		return false
	}
	if t.Slot != o.Slot {
		return false
	}
	return true
}

func (t *UnaryNopNop) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *UnaryNopNop) String() string {
	return fmt.Sprintf("UnaryNop(%v)", t.Slot)
}

func (t *UnaryNopNop) ChildCount() int { return 1 }

func (t *UnaryNopNop) ChildAt(i int) any {
	switch i {
	case 0:
		return t.Slot
	}
	panic(fmt.Sprintf("UnaryNopNop.ChildAt: index %d out of range", i))
}

func (t *UnaryNopNop) SetChildAt(i int, child any) any {
	switch i {
	case 0:
		return MakeUnaryNop(child.(Nop))
	}
	panic(fmt.Sprintf("UnaryNopNop.SetChildAt: index %d out of range", i))
}

func (t *UnaryNopNop) Children() []any {
	return []any{t.Slot}
}

func (t *UnaryNopNop) SetChildren(children []any) any {
	return MakeUnaryNop(children[0].(Nop))
}

// MakeUnaryNop builds the canonical (shared) UnaryNop term.
func MakeUnaryNop(slot Nop) Nop {
	hashes := []uint32{slot.Hash()}
	proto := &UnaryNopNop{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("UnaryNop"), hashes), Slot: slot}
	return factory.Build(proto).(*UnaryNopNop)
}

// IsUnaryNop is the `Is_UnaryNop` predicate strategy: succeeds (returns subject
// unchanged) when subject has the `UnaryNop` shape, otherwise fails with
// sl.ErrVisitFailure.
type IsUnaryNop struct{}

func (IsUnaryNop) VisitLight(subject any, _ sl.Introspector) (any, error) {
	if _, ok := subject.(*UnaryNopNop); ok {
		return subject, nil
	}
	return subject, sl.ErrVisitFailure
}
func (IsUnaryNop) ChildCount() int             { return 0 }
func (IsUnaryNop) ChildAt(int) sl.Strategy     { panic("IsUnaryNop: no children") }
func (IsUnaryNop) SetChildAt(int, sl.Strategy) { panic("IsUnaryNop: no children") }

// VisitUnaryNop is the `_UnaryNop` slot-visit strategy: when subject is `UnaryNop`,
// applies each constituent strategy to the matching child slot and
// rebuilds the term iff at least one child changed. Fails with
// sl.ErrVisitFailure when subject isn't `UnaryNop`.
type VisitUnaryNop struct {
	args []sl.Strategy
}

// NewVisitUnaryNop builds the slot-visit strategy with the supplied per-slot
// sub-strategies.
func NewVisitUnaryNop(args ...sl.Strategy) *VisitUnaryNop {
	return &VisitUnaryNop{args: args}
}

func (s *VisitUnaryNop) VisitLight(subject any, intro sl.Introspector) (any, error) {
	if _, ok := subject.(*UnaryNopNop); !ok {
		return subject, sl.ErrVisitFailure
	}
	if len(s.args) != 1 {
		return subject, sl.ErrVisitFailure
	}
	var newChildren []any
	for i := 0; i < 1; i++ {
		oldChild := intro.GetChildAt(subject, i)
		newChild, err := s.args[i].VisitLight(oldChild, intro)
		if err != nil {
			return subject, err
		}
		if newChildren != nil {
			newChildren[i] = newChild
		} else if newChild != oldChild {
			newChildren = intro.GetChildren(subject)
			newChildren[i] = newChild
		}
	}
	if newChildren != nil {
		return intro.SetChildren(subject, newChildren), nil
	}
	return subject, nil
}

func (s *VisitUnaryNop) ChildCount() int                 { return len(s.args) }
func (s *VisitUnaryNop) ChildAt(i int) sl.Strategy       { return s.args[i] }
func (s *VisitUnaryNop) SetChildAt(i int, v sl.Strategy) { s.args[i] = v }

// BinaryNopNop is the term type for the alternative `BinaryNop(...)` of sort Nop.
type BinaryNopNop struct {
	Ls   Nop
	Rs   Nop
	hash uint32
}

func (*BinaryNopNop) isNop() {}

func (t *BinaryNopNop) Hash() uint32 { return t.hash }

func (t *BinaryNopNop) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*BinaryNopNop)
	if !ok {
		return false
	}
	if t.Ls != o.Ls {
		return false
	}
	if t.Rs != o.Rs {
		return false
	}
	return true
}

func (t *BinaryNopNop) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *BinaryNopNop) String() string {
	return fmt.Sprintf("BinaryNop(%v,%v)", t.Ls, t.Rs)
}

func (t *BinaryNopNop) ChildCount() int { return 2 }

func (t *BinaryNopNop) ChildAt(i int) any {
	switch i {
	case 0:
		return t.Ls
	case 1:
		return t.Rs
	}
	panic(fmt.Sprintf("BinaryNopNop.ChildAt: index %d out of range", i))
}

func (t *BinaryNopNop) SetChildAt(i int, child any) any {
	switch i {
	case 0:
		return MakeBinaryNop(child.(Nop), t.Rs)
	case 1:
		return MakeBinaryNop(t.Ls, child.(Nop))
	}
	panic(fmt.Sprintf("BinaryNopNop.SetChildAt: index %d out of range", i))
}

func (t *BinaryNopNop) Children() []any {
	return []any{t.Ls, t.Rs}
}

func (t *BinaryNopNop) SetChildren(children []any) any {
	return MakeBinaryNop(children[0].(Nop), children[1].(Nop))
}

// MakeBinaryNop builds the canonical (shared) BinaryNop term.
func MakeBinaryNop(ls Nop, rs Nop) Nop {
	hashes := []uint32{ls.Hash(), rs.Hash()}
	proto := &BinaryNopNop{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("BinaryNop"), hashes), Ls: ls, Rs: rs}
	return factory.Build(proto).(*BinaryNopNop)
}

// IsBinaryNop is the `Is_BinaryNop` predicate strategy: succeeds (returns subject
// unchanged) when subject has the `BinaryNop` shape, otherwise fails with
// sl.ErrVisitFailure.
type IsBinaryNop struct{}

func (IsBinaryNop) VisitLight(subject any, _ sl.Introspector) (any, error) {
	if _, ok := subject.(*BinaryNopNop); ok {
		return subject, nil
	}
	return subject, sl.ErrVisitFailure
}
func (IsBinaryNop) ChildCount() int             { return 0 }
func (IsBinaryNop) ChildAt(int) sl.Strategy     { panic("IsBinaryNop: no children") }
func (IsBinaryNop) SetChildAt(int, sl.Strategy) { panic("IsBinaryNop: no children") }

// VisitBinaryNop is the `_BinaryNop` slot-visit strategy: when subject is `BinaryNop`,
// applies each constituent strategy to the matching child slot and
// rebuilds the term iff at least one child changed. Fails with
// sl.ErrVisitFailure when subject isn't `BinaryNop`.
type VisitBinaryNop struct {
	args []sl.Strategy
}

// NewVisitBinaryNop builds the slot-visit strategy with the supplied per-slot
// sub-strategies.
func NewVisitBinaryNop(args ...sl.Strategy) *VisitBinaryNop {
	return &VisitBinaryNop{args: args}
}

func (s *VisitBinaryNop) VisitLight(subject any, intro sl.Introspector) (any, error) {
	if _, ok := subject.(*BinaryNopNop); !ok {
		return subject, sl.ErrVisitFailure
	}
	if len(s.args) != 2 {
		return subject, sl.ErrVisitFailure
	}
	var newChildren []any
	for i := 0; i < 2; i++ {
		oldChild := intro.GetChildAt(subject, i)
		newChild, err := s.args[i].VisitLight(oldChild, intro)
		if err != nil {
			return subject, err
		}
		if newChildren != nil {
			newChildren[i] = newChild
		} else if newChild != oldChild {
			newChildren = intro.GetChildren(subject)
			newChildren[i] = newChild
		}
	}
	if newChildren != nil {
		return intro.SetChildren(subject, newChildren), nil
	}
	return subject, nil
}

func (s *VisitBinaryNop) ChildCount() int                 { return len(s.args) }
func (s *VisitBinaryNop) ChildAt(i int) sl.Strategy       { return s.args[i] }
func (s *VisitBinaryNop) SetChildAt(i int, v sl.Strategy) { s.args[i] = v }

// TertiaryNopNop is the term type for the alternative `TertiaryNop(...)` of sort Nop.
type TertiaryNopNop struct {
	Ls   Nop
	Cs   Nop
	Rs   Nop
	hash uint32
}

func (*TertiaryNopNop) isNop() {}

func (t *TertiaryNopNop) Hash() uint32 { return t.hash }

func (t *TertiaryNopNop) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*TertiaryNopNop)
	if !ok {
		return false
	}
	if t.Ls != o.Ls {
		return false
	}
	if t.Cs != o.Cs {
		return false
	}
	if t.Rs != o.Rs {
		return false
	}
	return true
}

func (t *TertiaryNopNop) Duplicate() sharedobjects.Term {
	clone := *t
	return &clone
}

func (t *TertiaryNopNop) String() string {
	return fmt.Sprintf("TertiaryNop(%v,%v,%v)", t.Ls, t.Cs, t.Rs)
}

func (t *TertiaryNopNop) ChildCount() int { return 3 }

func (t *TertiaryNopNop) ChildAt(i int) any {
	switch i {
	case 0:
		return t.Ls
	case 1:
		return t.Cs
	case 2:
		return t.Rs
	}
	panic(fmt.Sprintf("TertiaryNopNop.ChildAt: index %d out of range", i))
}

func (t *TertiaryNopNop) SetChildAt(i int, child any) any {
	switch i {
	case 0:
		return MakeTertiaryNop(child.(Nop), t.Cs, t.Rs)
	case 1:
		return MakeTertiaryNop(t.Ls, child.(Nop), t.Rs)
	case 2:
		return MakeTertiaryNop(t.Ls, t.Cs, child.(Nop))
	}
	panic(fmt.Sprintf("TertiaryNopNop.SetChildAt: index %d out of range", i))
}

func (t *TertiaryNopNop) Children() []any {
	return []any{t.Ls, t.Cs, t.Rs}
}

func (t *TertiaryNopNop) SetChildren(children []any) any {
	return MakeTertiaryNop(children[0].(Nop), children[1].(Nop), children[2].(Nop))
}

// MakeTertiaryNop builds the canonical (shared) TertiaryNop term.
func MakeTertiaryNop(ls Nop, cs Nop, rs Nop) Nop {
	hashes := []uint32{ls.Hash(), cs.Hash(), rs.Hash()}
	proto := &TertiaryNopNop{hash: sharedobjects.MixSymbol(sharedobjects.StringHash("TertiaryNop"), hashes), Ls: ls, Cs: cs, Rs: rs}
	return factory.Build(proto).(*TertiaryNopNop)
}

// IsTertiaryNop is the `Is_TertiaryNop` predicate strategy: succeeds (returns subject
// unchanged) when subject has the `TertiaryNop` shape, otherwise fails with
// sl.ErrVisitFailure.
type IsTertiaryNop struct{}

func (IsTertiaryNop) VisitLight(subject any, _ sl.Introspector) (any, error) {
	if _, ok := subject.(*TertiaryNopNop); ok {
		return subject, nil
	}
	return subject, sl.ErrVisitFailure
}
func (IsTertiaryNop) ChildCount() int             { return 0 }
func (IsTertiaryNop) ChildAt(int) sl.Strategy     { panic("IsTertiaryNop: no children") }
func (IsTertiaryNop) SetChildAt(int, sl.Strategy) { panic("IsTertiaryNop: no children") }

// VisitTertiaryNop is the `_TertiaryNop` slot-visit strategy: when subject is `TertiaryNop`,
// applies each constituent strategy to the matching child slot and
// rebuilds the term iff at least one child changed. Fails with
// sl.ErrVisitFailure when subject isn't `TertiaryNop`.
type VisitTertiaryNop struct {
	args []sl.Strategy
}

// NewVisitTertiaryNop builds the slot-visit strategy with the supplied per-slot
// sub-strategies.
func NewVisitTertiaryNop(args ...sl.Strategy) *VisitTertiaryNop {
	return &VisitTertiaryNop{args: args}
}

func (s *VisitTertiaryNop) VisitLight(subject any, intro sl.Introspector) (any, error) {
	if _, ok := subject.(*TertiaryNopNop); !ok {
		return subject, sl.ErrVisitFailure
	}
	if len(s.args) != 3 {
		return subject, sl.ErrVisitFailure
	}
	var newChildren []any
	for i := 0; i < 3; i++ {
		oldChild := intro.GetChildAt(subject, i)
		newChild, err := s.args[i].VisitLight(oldChild, intro)
		if err != nil {
			return subject, err
		}
		if newChildren != nil {
			newChildren[i] = newChild
		} else if newChild != oldChild {
			newChildren = intro.GetChildren(subject)
			newChildren[i] = newChild
		}
	}
	if newChildren != nil {
		return intro.SetChildren(subject, newChildren), nil
	}
	return subject, nil
}

func (s *VisitTertiaryNop) ChildCount() int                 { return len(s.args) }
func (s *VisitTertiaryNop) ChildAt(i int) sl.Strategy       { return s.args[i] }
func (s *VisitTertiaryNop) SetChildAt(i int, v sl.Strategy) { s.args[i] = v }

// VaryNop is the term type for the alternative `Vary(...)` of sort Nop.
type VaryNop struct {
	Slots []Nop
	hash  uint32
}

func (*VaryNop) isNop() {}

func (t *VaryNop) Hash() uint32 { return t.hash }

func (t *VaryNop) Equivalent(other sharedobjects.Term) bool {
	o, ok := other.(*VaryNop)
	if !ok {
		return false
	}
	if len(t.Slots) != len(o.Slots) {
		return false
	}
	for i := range t.Slots {
		if t.Slots[i] != o.Slots[i] {
			return false
		}
	}
	return true
}

func (t *VaryNop) Duplicate() sharedobjects.Term {
	cp := append([]Nop(nil), t.Slots...)
	return &VaryNop{Slots: cp, hash: t.hash}
}

func (t *VaryNop) String() string {
	parts := make([]string, len(t.Slots))
	for i, v := range t.Slots {
		parts[i] = fmt.Sprintf("%v", v)
	}
	return "Vary" + "(" + strings.Join(parts, ",") + ")"
}

func (t *VaryNop) ChildCount() int { return len(t.Slots) }

func (t *VaryNop) ChildAt(i int) any { return t.Slots[i] }

func (t *VaryNop) SetChildAt(i int, child any) any {
	dup := append([]Nop(nil), t.Slots...)
	dup[i] = child.(Nop)
	return MakeVary(dup...)
}

func (t *VaryNop) Children() []any {
	out := make([]any, len(t.Slots))
	for i, v := range t.Slots {
		out[i] = v
	}
	return out
}

func (t *VaryNop) SetChildren(children []any) any {
	args := make([]Nop, len(children))
	for i, c := range children {
		args[i] = c.(Nop)
	}
	return MakeVary(args...)
}

// MakeVary builds the canonical (shared) Vary term.
func MakeVary(args ...Nop) Nop {
	hashes := make([]uint32, 0, len(args))
	for _, a := range args {
		hashes = append(hashes, a.Hash())
	}
	proto := &VaryNop{Slots: args, hash: sharedobjects.MixSymbol(sharedobjects.StringHash("Vary"), hashes)}
	return factory.Build(proto).(*VaryNop)
}

// IsVary is the `Is_Vary` predicate strategy: succeeds (returns subject
// unchanged) when subject has the `Vary` shape, otherwise fails with
// sl.ErrVisitFailure.
type IsVary struct{}

func (IsVary) VisitLight(subject any, _ sl.Introspector) (any, error) {
	if _, ok := subject.(*VaryNop); ok {
		return subject, nil
	}
	return subject, sl.ErrVisitFailure
}
func (IsVary) ChildCount() int             { return 0 }
func (IsVary) ChildAt(int) sl.Strategy     { panic("IsVary: no children") }
func (IsVary) SetChildAt(int, sl.Strategy) { panic("IsVary: no children") }

// VisitVary is the `_Vary` slot-visit strategy: when subject is `Vary`,
// applies each constituent strategy to the matching child slot and
// rebuilds the term iff at least one child changed. Fails with
// sl.ErrVisitFailure when subject isn't `Vary`.
type VisitVary struct {
	args []sl.Strategy
}

// NewVisitVary builds the slot-visit strategy with the supplied per-slot
// sub-strategies.
func NewVisitVary(args ...sl.Strategy) *VisitVary {
	return &VisitVary{args: args}
}

func (s *VisitVary) VisitLight(subject any, intro sl.Introspector) (any, error) {
	if _, ok := subject.(*VaryNop); !ok {
		return subject, sl.ErrVisitFailure
	}
	// Variadic alt: visit every element with args[0] (Java's
	// `_concX` invokes the sub-strategy on each list element).
	if len(s.args) == 0 {
		return subject, nil
	}
	count := intro.GetChildCount(subject)
	var newChildren []any
	for i := 0; i < count; i++ {
		oldChild := intro.GetChildAt(subject, i)
		newChild, err := s.args[0].VisitLight(oldChild, intro)
		if err != nil {
			return subject, err
		}
		if newChildren != nil {
			newChildren[i] = newChild
		} else if newChild != oldChild {
			newChildren = intro.GetChildren(subject)
			newChildren[i] = newChild
		}
	}
	if newChildren != nil {
		return intro.SetChildren(subject, newChildren), nil
	}
	return subject, nil
}

func (s *VisitVary) ChildCount() int                 { return len(s.args) }
func (s *VisitVary) ChildAt(i int) sl.Strategy       { return s.args[i] }
func (s *VisitVary) SetChildAt(i int, v sl.Strategy) { s.args[i] = v }

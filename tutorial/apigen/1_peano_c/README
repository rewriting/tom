* generate C files and Tom files:

  adt-to-c -i Peano.adt -j -v

* compile the Tom program:

  jtom --noWarning -c PeanoTest.t

* compile generated C files:

  gcc -c -I$(INSTALLDIR)/include PeanoTest.tom.c
  gcc -c -I$(INSTALLDIR)/include Peano.c
  gcc -c -I$(INSTALLDIR)/include Peano_dict.c
  gcc -o PeanoTest PeanoTest.tom.o Peano.o Peano_dict.o -L$(INSTALLDIR)/lib -lATerm
